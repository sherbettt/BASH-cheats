# Tor Browser в Manjaro Linux

##  Установка Tor Browser

### Способ 1: Установка из скачанного архива (рекомендуется)
1. Скачайте архив с официального сайта: https://www.torproject.org/download/ или https://tor.ybti.net/download/ (https://tor.ybti.net/dist/torbrowser/)
2. Распакуйте архив:
   ```bash
   tar -xf tor-browser-linux-x86_64-*.tar.xz
   ```
3. Переместите в `/opt`:
   ```bash
   sudo mv tor-browser /opt/
   ```

### Способ 2: Установка из AUR
```bash
yay -S tor-browser-bin
```

##  Настройка запуска

### 1. Создание алиаса в ~/.zshrc
```bash
echo "alias tor='cd /opt/tor-browser && ./start-tor-browser.desktop'" >> ~/.zshrc
source ~/.zshrc
```

### 2. Создание ярлыка в меню приложений
```bash
sudo nano /usr/share/applications/tor-browser.desktop
```
Добавить:
```ini
[Desktop Entry]
Type=Application
Name=Tor Browser
Exec=sh -c "cd /opt/tor-browser && ./start-tor-browser.desktop"
Icon=/opt/tor-browser/browser/chrome/icons/default/default128.png
Categories=Network;WebBrowser;
Terminal=false
```
Обновить кэш:
```bash
sudo update-desktop-database
```

### 3. Настройка прав доступа
```bash
chmod +x /opt/tor-browser/start-tor-browser.desktop
chmod +x /opt/tor-browser/Browser/start-tor-browser
chmod +x /opt/tor-browser/Browser/firefox
```

## 🌉 Настройка мостов (бриджей) для РФ

### Вариант A: При первом запуске
1. Нажать "Настроить"
2. Выбрать: "Мой интернет-провайдер блокирует подключение к Tor"
3. Выбрать тип моста:
   - **obfs4** - стандартный выбор
   - **snowflake** - рекомендуется для РФ
   - **meek-azure** - через облако Azure

### Вариант B: Получить мосты вручную
1. Перейти: https://bridges.torproject.org
2. Выбрать "obfs4"
3. Решить капчу
4. Скопировать полученные строки мостов
5. Вставить в настройках Tor Browser

### Вариант C: Snowflake (лучший для РФ)
```
snowflake 192.0.2.1:1
```

##  Дополнительные настройки

### Безопасность
1. Нажать на значок щита → выбрать уровень безопасности:
   - Стандартный
   - Безопасный (рекомендуется)
   - Наиболее безопасный
2. Включить HTTPS Everywhere (встроено)
3. Не менять размер окна браузера

### Для РФ также рекомендуется:
- Использовать snowflake или meek-azure мосты
- Включить DNS over HTTPS в настройках
- Регулярно обновлять мосты (раз в 1-2 месяца)

## 🔍 Проверка работы
1. Зайти на: https://check.torproject.org
2. Убедиться, что видна надпись "This browser is configured to use Tor"

## 🛠️ Команды для обслуживания

```bash
# Запуск Tor Browser
tor
# или
/opt/tor-browser/start-tor-browser.desktop

# Проверка процессов
ps aux | grep -i tor

# Обновление (для ручной установки)
# Скачать новую версию с сайта и повторить установку
```

##  Важные предупреждения
- Не устанавливайте дополнения в Tor Browser
- Не открывайте документы (PDF, DOC) через Tor
- Не меняйте размер окна браузера
- Всегда проверяйте, что используете HTTPS
- Для максимальной анонимности используйте Tails OS

## 📞 Если не работает
1. Проверить процессы: `ps aux | grep -i tor`
2. Запустить с отладкой: `./start-tor-browser.desktop --verbose`
3. Проверить права: `ls -la /opt/tor-browser/`
4. Обновить мосты





