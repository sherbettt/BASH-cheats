## текущее время и дата
[man date](https://manned.org/man/date)
<br/> [cheat.sh/date](https://cheat.sh/date)

### Базовые команды
- показать дату в формате UTC/GMT
  ```c
  ┌─ root ~
  └─ # date -u
  Вт 08 апр 2025 19:50:33 UTC
  ```
- показать дату в формате RFC 5322
  ```c
  ┌─ root ~
  └─ # date -R
  Tue, 08 Apr 2025 22:52:15 +0300
  ```
- показать дату в формате RFC 3339 (совместим с ISO 8601)
  ```c
  ┌─ root ~
  └─ # date --rfc-3339=seconds
  2025-04-08 22:52:55+03:00
  ┌─ root ~
  └─ # date --rfc-3339=date
  2025-04-08
  ```

### Форматирование времени
- показать время в формате ***%H:%M:%S***
  ```c
  ┌─ root ~
  └─ # date +%T
  22:52:55
  ```
- показать время в 12-часовом формате с AM/PM
  ```c
  ┌─ root ~
  └─ # date +%I:%M:%S %p
  10:52:55 PM
  ```
- показать полную дату в формате ISO
   <br/> `date +"%F"`
- показать только год, месяц, день
  ```c
  ┌─ root ~
  └─ # date +"%Y-%m-%d"
  2025-04-08
  ```
- показать имя временной зоны
  <br/> `date +"%Z"`
- показать смещение относительно UTC
  ```c
  ┌─ root ~
  └─ # date +"%z"
  +0300
  ```

### Локализованное отображение
- показать локализованное время
  <br/> `date +"%X"` , где
    - **%X** - отображение времени в регионе (например, 23:13:48)
- показать локализованную дату
  <br/> `date +"%x"` , где
    - **%x** - отображение даты в регионе (например, 08.04.2025)
- показать день недели
  ```c
  ┌─ root ~
  └─ # date +"%A"
  Tuesday
  ┌─ root ~
  └─ # date +"%a"
  Tue
  ```

### Форматы даты
- показать дату
<br/> `date +'%e.%m.%Y'` , где
    - **%e** - день месяца,
    - **%m** - месяц (01..12),
    - **%Y** - год
- показать дату
<br/> `date +'%a %d.%m.%g %R'` , где
    - **%a** - сокращенное название дня недели в регионе (например, Sun = Вс),
    - **%d** - день месяца (напр., 01),
    - **%g** - последние две цифры года в номере недели ISO (неоднозначны; 00-99); см. **%G**
    - **%R** - 24-часовой индикатор часов и минут; такой же, как ***%H:%M***
- показать дату с названием месяца
  ```c
  ┌─ root ~
  └─ # date +"%d %B %Y"
  08 April 2025
  ┌─ root ~
  └─ # date +"%d %b %Y"
  08 Apr 2025
  ```
- показать день года и неделю
  ```c
  ┌─ root ~
  └─ # date +"День %j, неделя %U"
  День 098, неделя 14
  ```

### Unix timestamp

#### Из Unix timestamp в дату
- Преобразует секунды, прошедшие с начала эпохи (1970-01-01 UTC), в дату
```c
┌─ root ~
└─ # date --date='@2147483647'
Вт 19 янв 2038 06:14:07 MSK
```
- Преобразовать заданную дату в формат временных меток Unix
  <br/> `date -d @1744141621`
```c
┌─ root ~
└─ # date -d @1744141621
Вт 08 апр 2025 22:47:01 MSK
```
- Преобразовать с форматированием
  ```c
  ┌─ root ~
  └─ # date -d @1744141621 +"%Y-%m-%d %H:%M:%S"
  2025-04-08 22:47:01
  ```
- Преобразовать в UTC
  ```c
  ┌─ root ~
  └─ # date -u -d @1744141621
  Вт 08 апр 2025 19:47:01 UTC
  ```

#### Из даты в Unix timestamp
- Отображение текущей даты в виде временной метки Unix (секунды с начала эпохи Unix)
  <br/> `date +%s`
```c
┌─ root ~
└─ # date +%s
1744141621
```
- Преобразовать конкретную дату в Unix timestamp
  ```c
  ┌─ root ~
  └─ # date -d "2025-04-08 22:47:01" +%s
  1744141621
  ```
- Преобразовать дату в UTC в Unix timestamp
  ```c
  ┌─ root ~
  └─ # date -u -d "2025-04-08 19:47:01" +%s
  1744141621
  ```
- Преобразовать различные форматы даты в timestamp
  ```c
  ┌─ root ~
  └─ # date -d "2025-04-08" +%s
  1744070400
  ┌─ root ~
  └─ # date -d "April 8, 2025" +%s
  1744070400
  ┌─ root ~
  └─ # date -d "08/04/2025" +%s
  1744070400
  ┌─ root ~
  └─ # date -d "8 Apr 2025 22:47:01" +%s
  1744141621
  ```
- Преобразовать относительную дату в timestamp
  ```c
  ┌─ root ~
  └─ # date -d "tomorrow 00:00:00" +%s
  1744156800
  ┌─ root ~
  └─ # date -d "next monday" +%s
  1744502400
  ┌─ root ~
  └─ # date -d "1 hour ago" +%s
  1744138021
  ```

#### Полезные операции с timestamp
- Получить время в миллисекундах
  ```c
  ┌─ root ~
  └─ # date +%s%3N
  1744141621123
  ```
- Получить время в наносекундах
  ```c
  ┌─ root ~
  └─ # date +%s%N
  1744141621123456789
  ```
- Вычислить разницу между двумя датами в секундах
  ```c
  ┌─ root ~
  └─ # echo $(($(date -d "2025-04-10" +%s) - $(date -d "2025-04-08" +%s)))
  172800
  ```
- Преобразовать timestamp в читаемый формат с помощью awk
  ```c
  ┌─ root ~
  └─ # echo 1744141621 | awk '{print strftime("%Y-%m-%d %H:%M:%S", $1)}'
  2025-04-08 22:47:01
  ```
- Преобразовать timestamp в читаемый формат с помощью perl
  ```c
  ┌─ root ~
  └─ # perl -e 'print scalar(localtime(1744141621)), "\n"'
  Tue Apr  8 22:47:01 2025
  ```

### Манипуляции с датами
- Прибавить/вычесть время
  ```c
  ┌─ root ~
  └─ # date -d "now + 2 days"
  Чт 10 апр 2025 22:52:55 MSK
  ┌─ root ~
  └─ # date -d "yesterday"
  Пн 07 апр 2025 22:52:55 MSK
  ┌─ root ~
  └─ # date -d "next month"
  Пт 09 мая 2025 22:52:55 MSK
  ┌─ root ~
  └─ # date -d "last year"
  Пн 08 апр 2024 22:52:55 MSK
  ┌─ root ~
  └─ # date -d "1 hour ago"
  Вт 08 апр 2025 21:52:55 MSK
  ┌─ root ~
  └─ # date -d "next monday"
  Пн 14 апр 2025 00:00:00 MSK
  ```
- Сравнение дат
  ```c
  ┌─ root ~
  └─ # date -d "2025-04-08 22:00:00" +%s
  1744146000
  ┌─ root ~
  └─ # date -d "2025-04-08 23:00:00" +%s
  1744149600
  ```

### Часовые пояса
- Показать время в другом часовом поясе
  ```c
  ┌─ root ~
  └─ # TZ=America/New_York date
  Tue 08 Apr 2025 15:52:55 EDT
  ┌─ root ~
  └─ # TZ=Asia/Tokyo date
  Wed 09 Apr 2025 04:52:55 JST
  ┌─ root ~
  └─ # TZ=Europe/London date
  Tue 08 Apr 2025 20:52:55 BST
  ```
- Список всех часовых поясов
  ```c
  ┌─ root ~
  └─ # timedatectl list-timezones | grep Moscow
  Europe/Moscow
  ```

### Примеры
```c
┌─ root ~
└─ # date +'Сегодня %e число %m месяца %Y года'
Сегодня  8 число 04 месяца 2025 года
```
```c
┌─ root ~
└─ # date +'%A, %d %B %Y, %H:%M:%S %Z'
Tuesday, 08 April 2025, 22:52:55 MSK
```
```c
┌─ root ~
└─ # date +'%Y%m%d_%H%M%S'
20250408_225255
```
```c
┌─ root ~
└─ # date +'%Y-%m-%d %H:%M:%S %z'
2025-04-08 22:52:55 +0300
```
```c
┌─ root ~
└─ # date +'Сегодня %A, %d.%m.%Y, сейчас %H:%M:%S'
Сегодня Tuesday, 08.04.2025, сейчас 22:52:55
```
```c
┌─ root ~
└─ # echo "Текущий timestamp: $(date +%s), через час: $(date -d '1 hour' +%s)"
Текущий timestamp: 1744141621, через час: 1744145221
```

### Информация о системе и времени
```c
┌─ root ~
└─ # timedatectl
               Local time: Вт 2025-04-08 22:55:50 MSK
           Universal time: Вт 2025-04-08 19:55:50 UTC
                 RTC time: Вт 2025-04-08 19:55:50
                Time zone: Europe/Moscow (MSK, +0300)
System clock synchronized: yes
              NTP service: active
          RTC in local TZ: no
```
```c
┌─ root ~
└─ # chronyc tracking
Reference ID    : 5F4F1E71 (95x79x30x113.static-customer.nn.ertelecom.ru)
Stratum         : 3
Ref time (UTC)  : Tue Apr 08 19:48:43 2025
System time     : 3.840163946 seconds fast of NTP time
Last offset     : -0.000419706 seconds
RMS offset      : 0.041392166 seconds
Frequency       : 100037.547 ppm fast
Residual freq   : -0.002 ppm
Skew            : 0.227 ppm
Root delay      : 0.027464764 seconds
Root dispersion : 0.011804827 seconds
Update interval : 1029.2 seconds
Leap status     : Normal
```
```c
┌─ root ~
└─ # ls -l /etc/localtime
lrwxrwxrwx 1 root root 33 Apr  8 19:48 /etc/localtime -> /usr/share/zoneinfo/Europe/Moscow
```
```c
┌─ root ~
└─ # cat /etc/timezone
Europe/Moscow
```
```c
┌─ root ~
└─ # ntpq -np
     remote           refid      st t when poll reach   delay   offset  jitter
==============================================================================
*95x79x30x113.st 10.0.0.1         3 u  973 1024  377   27.464   -0.419  41.392
```
```c
┌─ root ~
└─ # hwclock --show
2025-04-08 22:55:50.123456+03:00
```

### Полезные алиасы
Добавьте в `~/.bashrc` или `~/.bash_aliases`:
```bash
alias now='date +"%Y-%m-%d %H:%M:%S %Z"'
alias timestamp='date +%s'
alias isodate='date --rfc-3339=seconds'
alias utc='date -u'
alias week='date +"Неделя %U, день %j"'
alias tomorrow='date -d "tomorrow" +"%A, %d.%m.%Y"'
alias ts2date='date -d @'
alias date2ts='date +%s -d'
```

### Скрипты для работы с timestamp
- Функция для преобразования timestamp в дату:
```bash
function ts() {
    if [ -z "$1" ]; then
        echo "Текущий timestamp: $(date +%s)"
    else
        date -d "@$1" +"%Y-%m-%d %H:%M:%S %Z"
    fi
}
```
- Функция для преобразования даты в timestamp:
```bash
function tots() {
    if [ -z "$1" ]; then
        echo "Использование: tots '2025-04-08 22:47:01'"
    else
        date -d "$1" +%s
    fi
}
```

---

