Имеем исходную часть с примером struct, где добавили Person:
```go
package main

import "fmt"

type Vertex struct {
	X int
	Y int
}

type Person struct {
    Name string
    Age  int
    City string
}

func main() {
	fmt.Println(Vertex{1, 2})
	fmt.Println(Person{Name: "Alice", Age: 30, City: "Moscow"})
}
```

Функция, которая просто выводит
```go
package main

import "fmt"

type Person struct {
    Name string
    Age  int
    City string
}

// Отдельная функция для вывода Person
func printPerson(p Person) {
    fmt.Println("Имя:", p.Name)
    fmt.Println("Возраст:", p.Age)
    fmt.Println("Город:", p.City)
}

func main() {
    alice := Person{Name: "Alice", Age: 30, City: "Moscow"}
    printPerson(alice) // вызываем нашу функцию
}
```

Функция, которая возвращает строку
```go
package main

import "fmt"

type Person struct {
    Name string
    Age  int
    City string
}

// Функция возвращает строковое представление Person
func personInfo(p Person) string {
    return fmt.Sprintf("Person{Name: %s, Age: %d, City: %s}", p.Name, p.Age, p.City)
}

func main() {
    alice := Person{Name: "Alice", Age: 30, City: "Moscow"}
    fmt.Println(personInfo(alice))
}
```

Функция, которая создает Person
```go
package main

import "fmt"

type Person struct {
    Name string
    Age  int
    City string
}

// Функция-конструктор (создает Person)
func newPerson(name string, age int, city string) Person {
    return Person{Name: name, Age: age, City: city}
}

func main() {
    alice := newPerson("Alice", 30, "Moscow")
    fmt.Println(alice)
}
```
