
В Proxmox VE утилита `pct` (Proxmox Container Toolkit) предназначена для управления LXC-контейнерами. Вот основные команды, которые могут быть полезны:  

---

### **1. Общее управление контейнерами**  
| Команда | Описание |
|---------|----------|
| `pct list` | Список всех контейнеров (аналог `pct status`) |
| `pct start <CTID>` | Запустить контейнер |
| `pct stop <CTID>` | Остановить контейнер (мягко) |
| `pct shutdown <CTID>` | Корректное завершение (аналог `stop`) |
| `pct reboot <CTID>` | Перезагрузить контейнер |
| `pct destroy <CTID>` | Удалить контейнер (безвозвратно!) |
| `pct suspend <CTID>` | Приостановить (заморозить) контейнер |
| `pct resume <CTID>` | Возобновить работу контейнера |

---

### **2. Конфигурация контейнеров**  
| Команда | Описание |
|---------|----------|
| `pct config <CTID>` | Показать текущую конфигурацию |
| `pct set <CTID> --<параметр> <значение>` | Изменить параметр (например, `--memory 2048`) |
| `pct unlock <CTID>` | Разблокировать контейнер (если заблокирован) |
| `pct resize <CTID> <диск> <размер>` | Изменить размер диска (например, `resize 100 rootfs +5G`) |

---

### **3. Сеть**  
| Команда | Описание |
|---------|----------|
| `pct set <CTID> --net0 <настройки>` | Настроить сетевой интерфейс (например, `name=eth0,bridge=vmbr0`) |
| `pct set <CTID> --nameserver <DNS>` | Установить DNS (например, `8.8.8.8`) |
| `pct set <CTID> --searchdomain <домен>` | Установить домен поиска |

---

### **4. Ресурсы и ограничения**  
| Команда | Описание |
|---------|----------|
| `pct set <CTID> --memory <МБ>` | Ограничить RAM (например, `--memory 2048`) |
| `pct set <CTID> --cores <N>` | Ограничить CPU (количество ядер) |
| `pct set <CTID> --swap <МБ>` | Установить размер swap |
| `pct set <CTID> --cpuunits <вес>` | Приоритет CPU (по умолчанию `1024`) |

---

### **5. Автозагрузка и порядок запуска**  
| Команда | Описание |
|---------|----------|
| `pct set <CTID> --onboot 1` | Включить автозагрузку |
| `pct set <CTID> --onboot 0` | Отключить автозагрузку |
| `pct set <CTID> --startup "order=N,up=M,down=K"` | Порядок загрузки (например, `order=1,up=30`) |

---

### **6. Резервное копирование и миграция**  
| Команда | Описание |
|---------|----------|
| `pct backup <CTID> <архив>` | Создать резервную копию (например, `backup 100 vzdump-lxc-100.tar.gz`) |
| `pct restore <архив> <CTID>` | Восстановить из резервной копии |
| `pct migrate <CTID> <целевой_узел>` | Мигрировать контейнер на другой узел |

---

### **7. Доступ к консоли и файлам**  
| Команда | Описание |
|---------|----------|
| `pct enter <CTID>` | Войти в консоль контейнера (аналог `lxc-attach`) |
| `pct console <CTID>` | Подключиться через веб-консоль |
| `pct push <CTID> <локальный_файл> <путь_в_контейнере>` | Загрузить файл в контейнер |
| `pct pull <CTID> <файл_в_контейнере> <локальный_путь>` | Скачать файл из контейнера |

---

### **8. Шаблоны и ОС**  
| Команда | Описание |
|---------|----------|
| `pct create <CTID> <шаблон> --ostype <тип_ОС>` | Создать контейнер из шаблона (например, `local:vztmpl/ubuntu-22.04-standard_22.04-1_amd64.tar.gz`) |
| `pct template <CTID>` | Создать шаблон из контейнера |

---

### **9. Мониторинг и статистика**  
| Команда | Описание |
|---------|----------|
| `pct exec <CTID> <команда>` | Выполнить команду внутри контейнера |
| `pct top <CTID>` | Показать процессы (аналог `top`) |
| `pct df <CTID>` | Показать использование диска |

---

### **10. Примеры использования**  
#### **Создание контейнера**  
```bash
pct create 100 local:vztmpl/ubuntu-22.04-standard_22.04-1_amd64.tar.gz \
  --storage local-lvm --memory 2048 --cores 2 --hostname mycontainer
```

#### **Изменение параметров**  
```bash
pct set 100 --memory 4096 --cores 4 --onboot 1
pct set ${containerId} -net0 name=eth0,bridge=vmbr0,ip=${staticIp}/24,gw=192.168.1.1
```

#### **Резервное копирование**  
```bash
pct backup 100 /var/lib/vz/dump/vzdump-lxc-100-2023.tar.gz
```
<br/>





### **🔹 Основные команды Proxmox VE (через CLI)**  

#### **1. Управление нодами (узлами)**
| Команда | Описание |
|---------|----------|
| `pvecm status` | Проверить статус кластера |
| `pvecm nodes` | Список узлов в кластере |
| `pvecm add <node>` | Добавить узел в кластер |
| `pvecm delnode <node>` | Удалить узел из кластера |

#### **2. Управление виртуальными машинами (KVM)**
| Команда | Описание |
|---------|----------|
| `qm list` | Список всех ВМ |
| `qm start <VMID>` | Запустить ВМ |
| `qm stop <VMID>` | Остановить ВМ |
| `qm shutdown <VMID>` | Корректное выключение ВМ |
| `qm destroy <VMID>` | Удалить ВМ |
| `qm suspend <VMID>` | Приостановить ВМ |
| `qm resume <VMID>` | Возобновить ВМ |
| `qm clone <VMID> <newVMID>` | Клонировать ВМ |

#### **3. Управление контейнерами (LXC)**
| Команда | Описание |
|---------|----------|
| `pct list` | Список контейнеров |
| `pct start <CTID>` | Запустить контейнер |
| `pct stop <CTID>` | Остановить контейнер |
| `pct shutdown <CTID>` | Корректное выключение |
| `pct destroy <CTID>` | Удалить контейнер |
| `pct enter <CTID>` | Войти в контейнер |

#### **4. Управление хранилищами**
| Команда | Описание |
|---------|----------|
| `pvesm status` | Статус хранилищ |
| `pvesm list <storage>` | Список файлов в хранилище |
| `pvesm add ...` | Добавить хранилище |

#### **5. Сеть**
| Команда | Описание |
|---------|----------|
| `cat /etc/network/interfaces` | Просмотр сетевых настроек |
| `ip a` | Список сетевых интерфейсов |
| `pct set <CTID> --net0 ...` | Настроить сеть контейнера |

#### **6. Автозагрузка контейнера**
```c
root@pmx5 ~ # pct set 18
182  183  184  
root@pmx5 ~ # pct set 184 --onboot 1
root@pmx5 ~ # pct config 184 | grep onboot
onboot: 1
root@pmx5 ~ # cat /etc/pve/lxc/184.conf | grep onboot
onboot: 1

root@pmx6 ~ # pct list
VMID       Status     Lock         Name                
102        running                 dmzgateway          
104        stopped                 aiohttp-test        
105        running                 wllm                
110        running                 deb12-builder       
113        stopped                 test-gw             
115        running                 app3                
123        running                 deb11-builder       
128        running                 dev71               
131        stopped                 test-lan2           
132        stopped                 cluster-test1       
134        stopped                 cluster-test2       
136        stopped                 cluster-test3       
139        running                 floatip1            
141        running                 floatip2            
142        running                 wikijs              
143        running                 dm-pbx              
144        stopped                 docusaurus          
148        running                 lk-test3            
173        running                 deb10-builder       
185        running                 jira-node2          
202        running                 pg2                 
272        running                 keycloack

root@pmx6 ~ # systemctl status pve-container@102

pct set 184 --startup "order=1,up=30,down=30"
pct config 184 | grep startup
```


---

### **📊 Таблица "мест" (статус узлов и ресурсов)**  

Пример вывода `pvecm status`:  

```
root@pve1:~# pvecm status  
Cluster information  
------------------  
Name:             mycluster  
Config Version:   3  
Transport:        knet  
Nodes:            3  
Quorum:           Yes  
Node ID:          1  
Ring ID:          1/1  
```

Пример вывода `qm list` (список ВМ):  

```
┌─────┬──────────────┬──────┬────────┬─────┬─────────┬──────────────┐  
│ VMID │ Name        │ Status │ Mem    │ CPU │ Storage │ Notes        │  
├─────┼──────────────┼──────┼────────┼─────┼─────────┼──────────────┤  
│ 100 │ Ubuntu-22.04 │ running │ 4096   │ 2   │ local   │ Production   │  
│ 101 │ Win10-Pro   │ stopped │ 8192   │ 4   │ nfs     │ Test         │  
└─────┴──────────────┴──────┴────────┴─────┴─────────┴──────────────┘  
```

---

### **🔹 Дополнительные полезные команды**  
- **Логи и мониторинг:**  
  ```bash
  journalctl -u pve-cluster -f  # Логи кластера
  dmesg | grep -i error         # Проверка ошибок ядра
  ```
- **Резервное копирование:**  
  ```bash
  vzdump <VMID> --mode snapshot --storage backup
  ```

---

Проверка логов контейнера
```bash
pct log 667
cat /var/log/syslog | grep 667
journalctl -u pve-container@667
```

Поиск событий вокруг времени остановки
```bash
journalctl --since "2024-12-10 16:50:00" --until "2024-12-10 17:10:00" | grep -B5 -A5 "667"
journalctl --since "2024-12-10 16:50:00" --until "2024-12-10 17:10:00" | grep -E "667|container|pve" | grep -v "Deactivated successfully"
```

Поиск в журнале задач Proxmox
```bash
cat /var/log/pve/tasks/active 2>/dev/null

# находим
# UPID:pmx5:001F1678:063189BB:69397D15:vzstop:667:ipetrov@runtel: 1 69397D16 OK
```

Преобразование времени полученное из логов.
```bash
# Преобразуем hex timestamp в читаемый формат
echo "69397D15" | awk '{printf "%d\n", "0x"$1}'
# Или
python3 -c "import time; print(time.strftime('%Y-%m-%d %H:%M:%S', time.gmtime(0x69397D15)))"

# Альтернативно через date
date -d @$(printf "%d" 0x69397D15)
```
Анализируем все действия пользователя ipetrov в то время
```bash
# Найдем все задачи, выполненные пользователем ipetrov
grep "ipetrov@runtel" /var/log/pve/tasks/active

# Посмотрим на полную запись с контекстом
grep -B2 -A2 "vzstop:667" /var/log/pve/tasks/active

# Проверим архивные задачи (если нужно больше истории)
find /var/log/pve/tasks/archive/ -name "*.log" -exec grep -l "ipetrov" {} \; 2>/dev/null
```
