# ПОЛНАЯ ИНСТРУКЦИЯ ПО НАСТРОЙКЕ SFTP СЕРВЕРА

## ОГЛАВЛЕНИЕ
1. [Часть 1: Настройка сервера (192.168.87.99 - backup)](#часть-1-настройка-сервера-1921688799---backup)
   - Шаг 1: Установка пакетов
   - Шаг 2: Создание директорий
   - Шаг 3: Создание групп пользователей
   - Шаг 4: Создание пользователей с ПОЛНЫМ доступом
   - Шаг 5: Создание пользователей ТОЛЬКО ДЛЯ ЧТЕНИЯ
   - Шаг 6: Настройка прав доступа (ВАЖНО!)
   - Шаг 7: Настройка SSH сервера (ВАЖНО!)
2. [Часть 2: Настройка клиентов (SSH ключи)](#часть-2-настройка-клиентов-ssh-ключи)
   - Шаг 8: Проверка существующих SSH ключей
   - Шаг 9: Добавление ключей на сервер
   - Шаг 10: Проверка прав на папки (КРИТИЧЕСКИ ВАЖНО!)
3. [Часть 3: Графические клиенты](#часть-3-графические-клиенты-для-пользователей)
4. [Часть 4: Настройка клиента для удобной работы](#часть-4-настройка-клиента-для-удобной-работы)
5. [Часть 5: Проверка работы и результаты тестов](#часть-5-проверка-работы-и-результаты-тестов)
6. [Часть 6: Автоматическое монтирование через SSHFS](#часть-6-автоматическое-монтирование-через-sshfs-для-удобства)
   - 6.1. Установка SSHFS
   - 6.2. Создание скриптов монтирования с правами dialog-llm
   - 6.3. Ручное монтирование
   - 6.4. Размонтирование
   - 6.5. Автоматическое монтирование после перезагрузки
7. [Таблица клиентов](#таблица-какие-клиенты-и-как-подключаться)
8. [Команды для проверки на сервере](#команды-для-проверки-на-сервере)
9. [Резюме](#резюме-где-что-выполнять)

---

## ЧАСТЬ 1: НАСТРОЙКА СЕРВЕРА (192.168.87.99 - backup)

### Шаг 1: Установка пакетов на сервере

**Где выполнять:** На машине `backup` (192.168.87.99)

```bash
#!/bin/bash
# ==========================================
# ВЫПОЛНЯЕТСЯ НА: backup (192.168.87.99)
# ==========================================

# Обновляем список пакетов
apt-get update

# Устанавливаем SSH сервер (если не установлен)
# openssh-server - сам SSH сервер
# sshfs - для монтирования (понадобится позже)
apt-get install -y openssh-server sshfs

# Проверяем, что SSH сервер работает
systemctl status sshd
# Если неактивен: systemctl start sshd
```

---

### Шаг 2: Создание директорий на сервере

**Где выполнять:** На машине `backup` (192.168.87.99)

```bash
#!/bin/bash
# ==========================================
# ВЫПОЛНЯЕТСЯ НА: backup (192.168.87.99)
# ==========================================

# Создаем основную корневую папку для SFTP
# mkdir -p создает папку и все промежуточные, если их нет
mkdir -p /sftp-share

# Создаем рабочую папку внутри - сюда будут ходить пользователи
# Это та самая папка, которую увидят клиенты
mkdir -p /sftp-share/sftp-share-dir

# Создаем папку для временных файлов
mkdir -p /sftp-share/temp

# ==========================================
# НАСТРОЙКА ПРАВ ДОСТУПА
# ==========================================

# Корневая папка должна принадлежать root (требование chroot)
# chown - меняет владельца
# root:root - владелец:группа
chown root:root /sftp-share

# chmod 755 - права: rwxr-xr-x
# владелец (root) - полный доступ (rwx)
# группа (root) - чтение и выполнение (r-x)
# остальные - чтение и выполнение (r-x)
chmod 755 /sftp-share

# Рабочая папка пока принадлежит root
chown root:root /sftp-share/sftp-share-dir
chmod 755 /sftp-share/sftp-share-dir
```

**Что означают права доступа (chmod):**
```
755 = rwx r-x r-x
      │   │   │
      │   │   └─── остальные: чтение + выполнение
      │   └─────── группа: чтение + выполнение
      └─────────── владелец: чтение + запись + выполнение

775 = rwx rwx r-x
      │   │   │
      │   │   └─── остальные: чтение + выполнение
      │   └─────── группа: чтение + запись + выполнение
      └─────────── владелец: чтение + запись + выполнение
```

---

### Шаг 3: Создание групп пользователей на сервере

**Где выполнять:** На машине `backup` (192.168.87.99)

```bash
#!/bin/bash
# ==========================================
# ВЫПОЛНЯЕТСЯ НА: backup (192.168.87.99)
# ==========================================

# groupadd - создает новую группу
# Группа для пользователей с ПОЛНЫМ доступом (чтение/запись/удаление)
groupadd sftp-full-access

# Группа для пользователей только с ЧТЕНИЕМ
groupadd sftp-readonly

# (ОПЦИОНАЛЬНО) Создать группу с конкретным GID
groupadd -g 1500 sftp-full-access
groupadd -g 1501 sftp-readonly

# Проверяем, что группы создались
# grep - ищет строки с "sftp-" в файле /etc/group
grep sftp- /etc/group
# Должны увидеть:
# sftp-full-access:x:1001:
# sftp-readonly:x:1002:
```

**Другие полезные опции для groupadd:**
```bash
# -g GID   : задать конкретный номер группы
# -r       : создать системную группу (GID < 1000)
# -f       : не выдавать ошибку, если группа уже существует
# -K KEY=VALUE : переопределить настройки по умолчанию

# Примеры:
groupadd -g 2000 sftp-full-access     # GID = 2000
groupadd -r sftp-system               # системная группа
groupadd -f sftp-full-access          # не выдаст ошибку, если существует
```

---

### Шаг 4: Создание пользователей с ПОЛНЫМ доступом на сервере

**Где выполнять:** На машине `backup` (192.168.87.99)

```bash
#!/bin/bash
# ==========================================
# ВЫПОЛНЯЕТСЯ НА: backup (192.168.87.99)
# ==========================================

# ==========================================
# РАЗБОР КОМАНДЫ useradd:
# ==========================================
# -m              : создать домашнюю папку
# -d ПУТЬ         : указать путь к домашней папке
# -g ГРУППА       : основная группа пользователя
# -s ОБОЛОЧКА     : командная оболочка (shell)
# -c "КОММЕНТАРИЙ": комментарий к пользователю
# ИМЯ_ПОЛЬЗОВАТЕЛЯ: логин пользователя
# ==========================================

# Пользователь для машины 192.168.87.118 (ai2)
useradd -m \
        -d /sftp-share/sftp-share-dir \
        -g sftp-full-access \
        -s /usr/sbin/nologin \
        -c "SFTP user for AI2 machine (192.168.87.118)" \
        sftp-ai2

# Пользователь для машины 192.168.87.179 (runelfrontdev)
useradd -m \
        -d /sftp-share/sftp-share-dir \
        -g sftp-full-access \
        -s /usr/sbin/nologin \
        -c "SFTP user for runelfrontdev machine (192.168.87.179)" \
        sftp-frontdev

# ==========================================
# Почему /usr/sbin/nologin?
# ==========================================
# Это специальная оболочка, которая запрещает вход по SSH.
# Пользователь сможет только подключаться по SFTP.
# При попытке ssh он увидит:
# "This account is currently not available."

# ==========================================
# УСТАНОВКА ПАРОЛЕЙ
# ==========================================

# passwd ИМЯ - устанавливает пароль для пользователя
# Будет запрошен ввод пароля (дважды для подтверждения)
echo "=== Установите пароль для sftp-ai2 ==="
passwd sftp-ai2

echo "=== Установите пароль для sftp-frontdev ==="
passwd sftp-frontdev

# Проверяем, что пользователи созданы
# id - показывает информацию о пользователе
id sftp-ai2
# Вывод: uid=1001(sftp-ai2) gid=1001(sftp-full-access) groups=1001(sftp-full-access)

id sftp-frontdev
# Вывод: uid=1002(sftp-frontdev) gid=1001(sftp-full-access) groups=1001(sftp-full-access)
```

#### Предупреждения useradd (НЕ ОШИБКИ!)

```bash
useradd: warning: the home directory /sftp-share/sftp-share-dir already exists.
useradd: Not copying any file from skel directory into it.
```

**Это НЕ ошибка**, а просто **предупреждение** (warning). Оно говорит:

- Папка `/sftp-share/sftp-share-dir` **УЖЕ СУЩЕСТВУЕТ** (мы ее создали ранее)
- `useradd` не стал копировать файлы из `/etc/skel/` (шаблон домашней папки)

**Почему это нормально:**
- Мы сами создали папку вручную (это правильно!)
- Для SFTP пользователей не нужны стандартные файлы `.bashrc`, `.profile` и т.д.
- Пользователь все равно не может зайти в shell (у него `/usr/sbin/nologin`)

---

### Шаг 5: Создание пользователей ТОЛЬКО ДЛЯ ЧТЕНИЯ на сервере

**Где выполнять:** На машине `backup` (192.168.87.99)

```bash
#!/bin/bash
# ==========================================
# ВЫПОЛНЯЕТСЯ НА: backup (192.168.87.99)
# ==========================================

# Создаем одного пользователя только для чтения
useradd -m \
        -d /sftp-share \
        -g sftp-readonly \
        -s /usr/sbin/nologin \
        -c "SFTP readonly user" \
        sftp-reader

# Устанавливаем пароль
passwd sftp-reader

# Можно создать дополнительных пользователей для других машин
# Например, если нужно дать доступ еще кому-то
useradd -m \
        -d /sftp-share \
        -g sftp-readonly \
        -s /usr/sbin/nologin \
        -c "SFTP readonly user for other machines" \
        sftp-other01

passwd sftp-other01
```

---

### Шаг 6: Настройка прав доступа (ВАЖНО!)

**Где выполнять:** На машине `backup` (192.168.87.99)

```bash
#!/bin/bash
# ==========================================
# ВЫПОЛНЯЕТСЯ НА: backup (192.168.87.99)
# ==========================================

# ==========================================
# НАСТРОЙКА ПРАВ НА ПАПКИ
# ==========================================

# Корневая папка - ТОЛЬКО root (требование chroot)
# Это БЕЗОПАСНОСТЬ: пользователи не могут выйти за пределы своей папки
chown root:root /sftp-share
chmod 755 /sftp-share

# ==========================================
# ВАЖНО! Правильные права на домашнюю папку
# ==========================================
# SSH требует, чтобы домашняя папка пользователя:
# 1. НЕ была доступна на запись для группы
# 2. Имела права НЕ БОЛЕЕ 755 (drwxr-xr-x)
# 
# Это требование БЕЗОПАСНОСТИ!
# Если группа может писать в домашнюю папку, то:
# - Другой пользователь в группе может добавить свой ключ
# - Или удалить ваши ключи
# - Это нарушает изоляцию пользователей

# РАБОЧАЯ папка - ТОЛЬКО ЧТЕНИЕ для группы (НЕ запись!)
# root: владелец root
# sftp-full-access: группа, у которой есть права ТОЛЬКО на чтение
chown root:sftp-full-access /sftp-share/sftp-share-dir

# 755 означает:
# rwx (владелец root) - полный доступ
# r-x (группа sftp-full-access) - ТОЛЬКО чтение и выполнение (НЕТ записи!)
# r-x (остальные) - только чтение
chmod 755 /sftp-share/sftp-share-dir

# ==========================================
# ПРАВИЛЬНЫЕ ПРАВА НА ВСЕ ПАПКИ:
# ==========================================
# /sftp-share/                         755 root:root
# /sftp-share/sftp-share-dir/          755 root:sftp-full-access (НЕ 775!)
# /sftp-share/sftp-share-dir/.ssh/     700 sftp-frontdev:sftp-full-access
# /sftp-share/sftp-share-dir/uploads/  775 sftp-ai2:sftp-full-access

# Создаем папку для загрузок с полными правами
mkdir -p /sftp-share/sftp-share-dir/uploads

# Папка uploads принадлежит пользователю sftp-ai2 и группе sftp-full-access
chown sftp-ai2:sftp-full-access /sftp-share/sftp-share-dir/uploads
chmod 775 /sftp-share/sftp-share-dir/uploads

# Создаем папку для логов
mkdir -p /var/log/sftp
chmod 755 /var/log/sftp

# Проверяем все права
echo "=== ПРОВЕРКА ПРАВ ==="
ll /sftp-share/
ll /sftp-share/sftp-share-dir/
```

**Почему права 755, а не 775 для домашней папки?**

| Права | Что означает | SSH |
|-------|--------------|-----|
| `775` (rwxrwxr-x) | Группа может писать | ❌ **НЕ РАБОТАЕТ** - SSH игнорирует ключи |
| `755` (rwxr-xr-x) | Группа ТОЛЬКО читает | ✅ **РАБОТАЕТ** - SSH принимает ключи |

**Ошибка, которая возникает при 775:**
```
debug1: Remote: Ignored authorized keys: bad ownership or modes for directory /sftp-share/sftp-share-dir
```

---

### Шаг 7: Настройка SSH сервера (ВАЖНО!)

**Где выполнять:** На машине `backup` (192.168.87.99)

```bash
#!/bin/bash
# ==========================================
# ВЫПОЛНЯЕТСЯ НА: backup (192.168.87.99)
# ==========================================

# Делаем резервную копию конфигурации SSH
# cp - копирует файл
# .backup - расширение для резервной копии
cp /etc/ssh/sshd_config /etc/ssh/sshd_config.backup
```

**ВАЖНО! НЕ ДУБЛИРУЙТЕ Subsystem!**

В файле `/etc/ssh/sshd_config` **УЖЕ ЕСТЬ** строка Subsystem. Нужно **ИЗМЕНИТЬ** ее на `internal-sftp`, а **НЕ ДОБАВЛЯТЬ** новую!

```bash
# ==========================================
# ИЗМЕНЯЕМ СУЩЕСТВУЮЩУЮ СТРОКУ Subsystem
# ==========================================

# Проверяем текущую строку Subsystem
grep "^Subsystem" /etc/ssh/sshd_config
# Должны увидеть: Subsystem sftp /usr/lib/openssh/sftp-server

# Способ 1: Через редактор (рекомендуется для надежности)
micro /etc/ssh/sshd_config
# ИЛИ
nano /etc/ssh/sshd_config

# Находим строку:
# Subsystem       sftp    /usr/lib/openssh/sftp-server
# Меняем на:
# Subsystem       sftp    internal-sftp

# Сохраняем и выходим

# Способ 2: Через sed (автоматически)
sed -i 's|/usr/lib/openssh/sftp-server|internal-sftp|' /etc/ssh/sshd_config

# Способ 3: Полная замена строки
sed -i '/^Subsystem/s/.*/Subsystem sftp internal-sftp/' /etc/ssh/sshd_config

# Проверяем, что изменилось
grep "^Subsystem" /etc/ssh/sshd_config
# Должно быть: Subsystem sftp internal-sftp
```

**Почему internal-sftp?**

`internal-sftp` - это встроенный SFTP сервер, который является частью SSH демона. В отличие от внешнего `/usr/lib/openssh/sftp-server`:

| Функция | Внешний sftp-server | internal-sftp |
|---------|-------------------|---------------|
| **Chroot** | ❌ НЕТ | ✅ ДА |
| **Изоляция** | ❌ НЕТ | ✅ ДА |
| **Производительность** | ⚠️ Медленнее (новый процесс) | ✅ Быстрее (внутри sshd) |
| **Безопасность** | ⚠️ Ниже | ✅ Выше |
| **Логирование** | ⚠️ Сложнее | ✅ Проще |
| **Ограничения** | ⚠️ Ограниченные | ✅ Полные |

Использование `internal-sftp` обязательно для работы `ChrootDirectory`!

**Затем ДОБАВЛЯЕМ ТОЛЬКО ПРАВИЛА MATCH (БЕЗ Subsystem!):**

```bash
# ==========================================
# ДОБАВЛЯЕМ НАСТРОЙКИ В КОНЕЦ ФАЙЛА
# ==========================================

cat >> /etc/ssh/sshd_config << 'EOF'

# ==========================================
# SFTP CHROOT RULES
# ==========================================

# Полный доступ для группы sftp-full-access
Match Group sftp-full-access
    # ChrootDirectory - "корневая" папка для пользователя
    # Пользователь не сможет выйти за ее пределы
    ChrootDirectory /sftp-share
    # ForceCommand - принудительно запускает только SFTP
    ForceCommand internal-sftp
    # Запрещаем интерактивный доступ
    PermitTTY no
    # Отключаем все лишние функции для безопасности
    PermitTunnel no
    AllowAgentForwarding no
    AllowTcpForwarding no
    X11Forwarding no
    # Разрешаем вход по паролю
    PasswordAuthentication yes
    # Таймаут сессии (если нет активности)
    ClientAliveInterval 60
    ClientAliveCountMax 3

# Только чтение для группы sftp-readonly
Match Group sftp-readonly
    ChrootDirectory /sftp-share
    # -R означает "только чтение" (Read-only)
    ForceCommand internal-sftp -R
    PermitTTY no
    PermitTunnel no
    AllowAgentForwarding no
    AllowTcpForwarding no
    X11Forwarding no
    PasswordAuthentication yes
    ClientAliveInterval 60
    ClientAliveCountMax 3

EOF
```

```bash
# ==========================================
# ПРОВЕРКА И ПЕРЕЗАПУСК
# ==========================================

# Проверяем, что Subsystem правильный
grep "^Subsystem" /etc/ssh/sshd_config
# Должно быть: Subsystem sftp internal-sftp

# Проверяем конфигурацию на ошибки
sshd -t
# Если команда ничего не вывела - ошибок нет!

# Перезапускаем SSH
systemctl restart ssh.service

# Проверяем статус
systemctl status ssh.service

# Проверяем, что настройки применились
sshd -T | grep -E "(subsystem|chrootdirectory|forcecommand)" | head -10
# Должны увидеть примерно:
# forcecommand none
# chrootdirectory none
# subsystem sftp internal-sftp
```

**Что означают опции в конфигурации SSH:**

| Опция | Значение |
|-------|----------|
| `Subsystem sftp internal-sftp` | Использовать встроенный SFTP сервер |
| `Match Group sftp-full-access` | Применить правила для группы |
| `ChrootDirectory /sftp-share` | "Корневая" папка для пользователя |
| `ForceCommand internal-sftp` | Принудительно запускать только SFTP |
| `PermitTTY no` | Запретить интерактивный доступ |
| `PasswordAuthentication yes` | Разрешить вход по паролю |
| `ClientAliveInterval 60` | Проверять соединение каждые 60 секунд |
| `ClientAliveCountMax 3` | Максимум 3 неудачных проверки |

---

## ЧАСТЬ 2: НАСТРОЙКА КЛИЕНТОВ (SSH КЛЮЧИ)

### Шаг 8: Проверка существующих SSH ключей

**Где выполнять:** На клиентских машинах (192.168.87.118 и 192.168.87.179)

```bash
# ==========================================
# ВЫПОЛНЯЕТСЯ НА: 192.168.87.118 (ai2) 
# И НА: 192.168.87.179 (runelfrontdev)
# ==========================================

# Проверяем, есть ли ключи
ls -la ~/.ssh/
# Если есть файлы id_rsa и id_rsa.pub - ключи уже существуют!

# Показываем публичный ключ (если есть)
cat ~/.ssh/id_rsa.pub
# Если ключ есть - используем его, НЕ создаем новый!
```

**Важно:** Если на клиентской машине уже есть SSH ключи, используйте их! Не создавайте новые.

### Шаг 9: Создание SSH ключей (только если нет существующих)

**Где выполнять:** На машине `192.168.87.118` (ai2) и `192.168.87.179` (runelfrontdev)

```bash
#!/bin/bash
# ==========================================
# ВЫПОЛНЯЕТСЯ НА: 
# 192.168.87.118 (ai2) И НА: 192.168.87.179 (runelfrontdev)
# ==========================================

# Генерируем SSH ключ для SFTP (только если нет ключей!)
# ssh-keygen - утилита для создания ключей
# -t rsa - тип ключа (RSA)
# -b 4096 - длина ключа в битах (4096 - очень надежно)
# -f ~/.ssh/id_rsa_sftp - файл для сохранения ключа
# -N "" - без парольной фразы (пустой пароль)
ssh-keygen -t rsa -b 4096 -f ~/.ssh/id_rsa_sftp -N ""

# После выполнения создадутся два файла:
# ~/.ssh/id_rsa_sftp - ПРИВАТНЫЙ ключ (НИКОМУ НЕ ПОКАЗЫВАТЬ!)
# ~/.ssh/id_rsa_sftp.pub - ПУБЛИЧНЫЙ ключ (можно показывать)
```

### Шаг 10: Добавление ключей на сервер (ВАЖНО! С ПРАВИЛЬНЫМИ ПРАВАМИ)

**Где выполнять:** На машине `backup` (192.168.87.99)

```bash
#!/bin/bash
# ==========================================
# ВЫПОЛНЯЕТСЯ НА: backup (192.168.87.99)
# ==========================================

# ==========================================
# ВАЖНО! Правильные права на папку .ssh
# ==========================================
# SSH требует:
# 1. Папка .ssh должна принадлежать пользователю, который подключается
# 2. Права 700 (drwx------)
# 3. Файл authorized_keys - права 600 (-rw-------)

# Создаем папку .ssh для пользователя sftp-frontdev
mkdir -p /sftp-share/sftp-share-dir/.ssh

# Владелец - sftp-frontdev (пользователь, который будет подключаться)
chown sftp-frontdev:sftp-full-access /sftp-share/sftp-share-dir/.ssh
chmod 700 /sftp-share/sftp-share-dir/.ssh

# Создаем файл authorized_keys
touch /sftp-share/sftp-share-dir/.ssh/authorized_keys
chown sftp-frontdev:sftp-full-access /sftp-share/sftp-share-dir/.ssh/authorized_keys
chmod 600 /sftp-share/sftp-share-dir/.ssh/authorized_keys
```

**Добавляем ВСЕ ключи в один файл:**

```bash
# ==========================================
# ДОБАВЛЯЕМ КЛЮЧИ В authorized_keys
# ==========================================

cat >> /sftp-share/sftp-share-dir/.ssh/authorized_keys << 'EOF'
# ==========================================
# КЛЮЧИ ДЛЯ ПОЛЬЗОВАТЕЛЯ sftp-frontdev
# ==========================================

# Ключ с ai2 (192.168.87.118)
ssh-rsa  root@ai2

# Ключ с frontdev (192.168.87.179)
ssh-rsa  root@runelfrontdev

# Ключ с вашей машины (kkorablin@Think)
ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIG4GcMVz9cKgtZHsEwGcy+Z2+mxhh6anhzm9IFzA1E2r kirill@kiko0217

# Insert SSH key of your local machine here and above
EOF

# ==========================================
# ПРОВЕРЯЕМ ВСЕ ПРАВА
# ==========================================

echo "=== ПРОВЕРКА ПРАВ НА ПАПКИ ==="
ll /sftp-share/
ll /sftp-share/sftp-share-dir/
ll /sftp-share/sftp-share-dir/.ssh/

echo "=== ПРОВЕРКА СОДЕРЖИМОГО ==="
cat /sftp-share/sftp-share-dir/.ssh/authorized_keys
```

**КРИТИЧЕСКИ ВАЖНО!** Финальная проверка прав:

```bash
# ==========================================
# ПРОВЕРКА ПРАВ (ДОЛЖНО БЫТЬ ТАК)
# ==========================================

# /sftp-share/
# drwxr-xr-x 4 root root /sftp-share/

# /sftp-share/sftp-share-dir/
# drwxr-xr-x 5 root sftp-full-access /sftp-share/sftp-share-dir/

# /sftp-share/sftp-share-dir/.ssh/
# drwx------ 2 sftp-frontdev sftp-full-access /sftp-share/sftp-share-dir/.ssh/

# /sftp-share/sftp-share-dir/.ssh/authorized_keys
# -rw------- 1 sftp-frontdev sftp-full-access /sftp-share/sftp-share-dir/.ssh/authorized_keys
```

**Если права не совпадают - SSH будет игнорировать ключи!**

Ошибка, которая возникает при неправильных правах:
```
debug1: Remote: Ignored authorized keys: bad ownership or modes for directory /sftp-share/sftp-share-dir
```

---

## ЧАСТЬ 3: ГРАФИЧЕСКИЕ КЛИЕНТЫ ДЛЯ ПОЛЬЗОВАТЕЛЕЙ

### Вариант 1: Double Commander (Рекомендую!)

**Double Commander** - отличный двухпанельный файловый менеджер, похожий на Total Commander.

```bash
# ==========================================
# УСТАНОВКА НА КЛИЕНТСКОЙ МАШИНЕ
# ==========================================

# Установка на Ubuntu/Debian
sudo apt-get update
sudo apt-get install -y doublecmd-qt

# Или через snap
sudo snap install double-commander

# Запуск
doublecmd
```

**Как подключиться к SFTP в Double Commander:**

1. Запустите Double Commander
2. Нажмите `Ctrl+N` или меню "Команды" → "Сетевое окружение"
3. Нажмите "Добавить" → "SFTP"
4. Заполните поля:

```
Имя подключения: Backup сервер
Хост: 192.168.87.99
Порт: 22
Имя пользователя: sftp-ai2 (или sftp-frontdev)
Пароль: [ваш пароль]
Путь: /sftp-share-dir
```

5. Нажмите "OK"
6. Дважды кликните на созданное подключение

**Скриншот-описание:**
```
+------------------------------------------+
| [🔗 Backup сервер]  [📁 Локальная]       |
+------------------------------------------+
|  📁 sftp-share-dir  |  📁 home           |
|  📄 file1.txt       |  📁 downloads      |
|  📄 file2.pdf       |  📁 documents      |
|  📁 uploads         |  📄 test.txt       |
+------------------------------------------+
|  [F5 Копировать] [F6 Переместить]        |
|  [F7 Создать] [F8 Удалить]               |
+------------------------------------------+
```

**Горячие клавиши в Double Commander:**
- `F5` - копировать файлы
- `F6` - переместить файлы
- `F7` - создать папку
- `F8` - удалить
- `Ctrl+C` / `Ctrl+V` - копировать/вставить

---

### Вариант 2: Dolphin (KDE)

**Dolphin** - стандартный файловый менеджер KDE.

```bash
# ==========================================
# УСТАНОВКА (если нет KDE)
# ==========================================

sudo apt-get update
sudo apt-get install -y dolphin

# Запуск
dolphin
```

**Как подключиться:**

1. Откройте Dolphin
2. В адресной строке введите:
   ```
   sftp://sftp-ai2@192.168.87.99/sftp-share-dir
   ```
3. Нажмите Enter
4. Введите пароль (если не настроены ключи)
5. Папка откроется как обычная локальная

**Создание закладки для быстрого доступа:**
1. Подключитесь к папке
2. Нажмите "Закладки" → "Добавить закладку"
3. Назовите: "Backup SFTP"

---

### Вариант 3: Nautilus (GNOME) - стандартный в Ubuntu

```bash
# ==========================================
# УСТАНОВКА (обычно уже установлен)
# ==========================================

# Проверяем, установлен ли
which nautilus

# Если нет - устанавливаем
sudo apt-get update
sudo apt-get install -y nautilus

# Запуск
nautilus
```

**Как подключиться:**

1. Откройте Nautilus (Files)
2. Нажмите "Other Locations" (Другие места) в левой панели
3. Внизу введите в поле "Connect to Server":
   ```
   sftp://sftp-ai2@192.168.87.99/sftp-share-dir
   ```
4. Нажмите "Connect"
5. Введите пароль

**Создание закладки:**
1. Подключитесь к папке
2. Нажмите `Ctrl+D` или меню "Bookmarks" → "Add Bookmark"
3. Теперь папка всегда будет в левой панели

---

### Вариант 4: FileZilla (Самый популярный FTP/SFTP клиент)

```bash
# ==========================================
# УСТАНОВКА
# ==========================================

sudo apt-get update
sudo apt-get install -y filezilla

# Запуск
filezilla
```

**Как подключиться:**

1. Откройте FileZilla
2. Нажмите `Ctrl+S` или File → Site Manager
3. Нажмите "New Site"
4. Назовите: "Backup SFTP"
5. Заполните:

```
Protocol:        SFTP - SSH File Transfer Protocol
Host:            192.168.87.99
Port:            22
Logon Type:      Normal
User:            sftp-ai2
Password:        [ваш пароль]
```

6. Нажмите "Connect"

---

### Вариант 5: Konqueror (KDE)

```bash
# ==========================================
# УСТАНОВКА
# ==========================================

sudo apt-get install -y konqueror

# Запуск
konqueror
```

**Как подключиться:**

В адресной строке:
```
sftp://sftp-ai2@192.168.87.99/sftp-share-dir
```

---

### Вариант 6: PCManFM (легкий файловый менеджер)

```bash
# ==========================================
# УСТАНОВКА
# ==========================================

sudo apt-get update
sudo apt-get install -y pcmanfm

# Запуск
pcmanfm
```

**Как подключиться:**

В адресной строке:
```
sftp://sftp-ai2@192.168.87.99/sftp-share-dir
```

---

## ЧАСТЬ 4: НАСТРОЙКА КЛИЕНТА ДЛЯ УДОБНОЙ РАБОТЫ

### Создание SSH конфига для быстрого подключения

**Где выполнять:** На клиентских машинах (192.168.87.118 и 192.168.87.179)

```bash
#!/bin/bash
# ==========================================
# ВЫПОЛНЯЕТСЯ НА: 192.168.87.118 (ai2)
# ==========================================

# Создаем конфигурационный файл SSH
cat >> ~/.ssh/config << 'EOF'
# ==========================================
# НАСТРОЙКИ ДЛЯ SFTP НА BACKUP
# ==========================================

Host backup-sftp
    HostName 192.168.87.99
    User sftp-ai2
    Port 22
    IdentityFile ~/.ssh/id_rsa
    StrictHostKeyChecking no
    UserKnownHostsFile /dev/null

Host backup-sftp-ai2
    HostName 192.168.87.99
    User sftp-ai2
    Port 22
    IdentityFile ~/.ssh/id_rsa

Host backup-sftp-frontdev
    HostName 192.168.87.99
    User sftp-frontdev
    Port 22
    IdentityFile ~/.ssh/id_rsa

EOF

# Теперь можно подключаться проще:
# sftp backup-sftp
# Или в файловом менеджере: sftp://backup-sftp/sftp-share-dir
```

**Для вашей личной машины (kkorablin@Think):**

```bash
# ==========================================
# НА ВАШЕЙ МАШИНЕ (kkorablin@Think)
# ==========================================

# Добавляем в ~/.ssh/config
cat >> ~/.ssh/config << 'EOF'

# Backup сервер (полный доступ)
Host backup-full
    HostName 192.168.87.99
    User sftp-frontdev
    Port 22
    IdentityFile ~/.ssh/id_ed25519
    StrictHostKeyChecking no

# Backup сервер (только чтение)
Host backup-read
    HostName 192.168.87.99
    User sftp-reader
    Port 22
    IdentityFile ~/.ssh/id_ed25519
    StrictHostKeyChecking no

# Короткий алиас для полного доступа
Host bf
    HostName 192.168.87.99
    User sftp-frontdev
    Port 22
    IdentityFile ~/.ssh/id_ed25519

# Короткий алиас для чтения
Host br
    HostName 192.168.87.99
    User sftp-reader
    Port 22
    IdentityFile ~/.ssh/id_ed25519

EOF

# Теперь подключаемся просто:
sftp backup-full   # полный доступ
sftp backup-read   # только чтение
sftp bf            # полный доступ (коротко)
sftp br            # только чтение (коротко)
```

---

## ЧАСТЬ 5: ПРОВЕРКА РАБОТЫ И РЕЗУЛЬТАТЫ ТЕСТОВ

### Проверка на клиенте (192.168.87.118 - ai2)

```bash
#!/bin/bash
# ==========================================
# ВЫПОЛНЯЕТСЯ НА: 192.168.87.118 (ai2)
# ==========================================

# 1. Проверка подключения через sftp
sftp sftp-ai2@192.168.87.99
# Если подключились - все работает

# 2. Проверка через ssh (должно быть отказано)
ssh sftp-ai2@192.168.87.99
# Ожидаемый результат: "This service allows sftp connections only."

# 3. Проверка создания файла и папки
echo "test" > test.txt
sftp sftp-ai2@192.168.87.99 <<EOF
put test.txt
mkdir test-folder
ls
rm test.txt
rmdir test-folder
bye
EOF
```

### Результаты тестов (как было проверено)

#### Тест 1: Подключение с 192.168.87.118 (ai2)

```bash
root@ai2 /opt/runtel/robot # sftp sftp-ai2@192.168.87.99
sftp-ai2@192.168.87.99's password: 
Connected to 192.168.87.99.
sftp> pwd
Remote working directory: /
sftp> cd sftp-share-dir/
sftp> mkdir test-two
sftp> ls -al
drwxrwxr-x    ? root     1012         4096 Aug 11 12:09 .
drwxr-xr-x    ? root     root         4096 Aug 11 11:15 ..
drwx------    ? 1012     1012         4096 Aug 11 12:06 .ssh
drwxr-xr-x    ? 1011     1012         4096 Aug 11 12:09 test-two
drwxrwxr-x    ? 1011     1012         4096 Aug 11 11:38 uploads
sftp> rmdir test-one/
sftp> exit
```

**Результат:** ✅ Пользователь может создавать и удалять папки

#### Тест 2: Проверка SSH (должен быть запрещен)

```bash
root@ai2 /opt/runtel/robot # ssh sftp-ai2@192.168.87.99
sftp-ai2@192.168.87.99's password: 
PTY allocation request failed on channel 0
This service allows sftp connections only.
Connection to 192.168.87.99 closed.
```

**Результат:** ✅ SSH доступ запрещен, только SFTP

#### Тест 3: Подключение с 192.168.87.179 (runelfrontdev)

```bash
root@runelfrontdev /opt/runtel/robot # sftp sftp-frontdev@192.168.87.99
sftp-frontdev@192.168.87.99's password: 
Connected to 192.168.87.99.
sftp> pwd
Remote working directory: /
sftp> cd sftp-share-dir/
sftp> mkdir test3
sftp> ls -al
drwxrwxr-x    ? root     1012         4096 Aug 11 12:11 .
drwxr-xr-x    ? root     root         4096 Aug 11 11:15 ..
drwx------    ? 1012     1012         4096 Aug 11 12:06 .ssh
drwxr-xr-x    ? 1012     1012         4096 Aug 11 12:11 test3
drwxrwxr-x    ? 1011     1012         4096 Aug 11 11:38 uploads
sftp> rmdir test-two/
sftp> exit
```

**Результат:** ✅ Второй пользователь тоже имеет полный доступ

#### Тест 4: Проверка read-only доступа

```bash
kkorablin@Think ~ # sftp sftp-reader@192.168.87.99
sftp-reader@192.168.87.99's password: 
Connected to 192.168.87.99.
sftp> cd sftp-share-dir/
sftp> ls -al
drwxrwxr-x    ? root     1012         4096 Aug 11 12:11 .
drwxr-xr-x    ? root     root         4096 Aug 11 11:15 ..
drwx------    ? 1012     1012         4096 Aug 11 12:06 .ssh
drwxr-xr-x    ? 1012     1012         4096 Aug 11 12:11 test3
drwxrwxr-x    ? 1011     1012         4096 Aug 11 11:38 uploads
sftp> rmdir test3/
remote rmdir "/sftp-share-dir/test3/": Permission denied
sftp> exit
```

**Результат:** ✅ Read-only пользователь НЕ МОЖЕТ удалять папки

#### Тест 5: Проверка авторизации по ключам (без пароля)

```bash
kkorablin@Think ~ # sftp -i ~/.ssh/id_ed25519 sftp-frontdev@192.168.87.99
Connected to 192.168.87.99.
sftp> exit
```

**Результат:** ✅ Подключение по ключу работает без пароля!

---

## ЧАСТЬ 6: АВТОМАТИЧЕСКОЕ МОНТИРОВАНИЕ ЧЕРЕЗ SSHFS (ДЛЯ УДОБСТВА)

### 6.1. Что такое SSHFS?

**SSHFS** (SSH Filesystem) - позволяет монтировать удаленную SFTP папку как локальный диск. После монтирования можно работать с файлами как с обычными локальными - открывать в редакторах, копировать, удалять через файловый менеджер.

### 6.2. Установка SSHFS на клиенте

```bash
# ==========================================
# НА КЛИЕНТСКОЙ МАШИНЕ (192.168.87.118 - ai2)
# ==========================================

# Устанавливаем sshfs
apt-get update
apt-get install -y sshfs

# Создаем локальную папку для монтирования
mkdir -p /opt/runtel/robot/sftp-share-dir
```

### 6.3. Создание скриптов монтирования с правами dialog-llm

**Важно:** Монтировать нужно с правами пользователя, который будет реально работать с папкой. В вашем случае это `dialog-llm` (UID=999, GID=996).

**Для 192.168.87.118 (ai2):**

```bash
# ==========================================
# НА 192.168.87.118 (ai2)
# ==========================================

# Создаем папку для скриптов
mkdir -p ~/scripts

# Создаем скрипт монтирования
micro ~/scripts/mount-sftp.sh
```

**Содержимое скрипта:**

```bash
#!/bin/bash
# ==========================================
# Скрипт для монтирования SFTP через sshfs
# С правами для пользователя dialog-llm (UID=999, GID=996)
# ==========================================

REMOTE_USER="sftp-ai2"
REMOTE_HOST="192.168.87.99"
REMOTE_PATH="/sftp-share-dir"
LOCAL_PATH="/opt/runtel/robot/sftp-share-dir"

# UID и GID пользователя dialog-llm
USER_UID=999
USER_GID=996

# Создаем локальную папку если не существует
mkdir -p "$LOCAL_PATH"

# Проверяем, смонтировано ли уже
if mount | grep -q "$LOCAL_PATH"; then
    echo "Папка уже смонтирована. Размонтируем..."
    fusermount -u "$LOCAL_PATH" 2>/dev/null || umount "$LOCAL_PATH"
    sleep 2
fi

# Монтируем с правами dialog-llm
echo "Монтируем SFTP папку с правами dialog-llm (UID=999, GID=996)..."
sshfs -o allow_other,reconnect,follow_symlinks,uid=$USER_UID,gid=$USER_GID \
      -o IdentityFile=~/.ssh/id_rsa \
      "$REMOTE_USER@$REMOTE_HOST:$REMOTE_PATH" "$LOCAL_PATH"

if [ $? -eq 0 ]; then
    echo "✅ SFTP смонтирован в $LOCAL_PATH"
    echo "Владелец: $(ls -ld $LOCAL_PATH | awk '{print $3":"$4}')"
    ls -la "$LOCAL_PATH"
else
    echo "❌ Ошибка монтирования"
    exit 1
fi
```

```bash
# Делаем скрипт исполняемым
chmod +x ~/scripts/mount-sftp.sh

# Запускаем монтирование
~/scripts/mount-sftp.sh
```

**Вариант для root:**
```bash
#!/bin/bash
# ==========================================
# Скрипт для монтирования SFTP через sshfs
# ~/scripts/mount-sftp-root.sh
# ==========================================

REMOTE_USER="sftp-ai2"
REMOTE_HOST="192.168.87.99"
REMOTE_PATH="/sftp-share-dir"
LOCAL_PATH="/opt/runtel/robot/sftp-share-dir"

# Создаем локальную папку если не существует
mkdir -p "$LOCAL_PATH"

# Проверяем, смонтировано ли уже
if mount | grep -q "$LOCAL_PATH"; then
    echo "Папка уже смонтирована. Размонтируем..."
    fusermount -u "$LOCAL_PATH" 2>/dev/null || umount "$LOCAL_PATH"
    sleep 2
fi

# Монтируем
echo "Монтируем SFTP папку..."
sshfs -o allow_other,reconnect,follow_symlinks \
      -o IdentityFile=~/.ssh/id_rsa \
      "$REMOTE_USER@$REMOTE_HOST:$REMOTE_PATH" "$LOCAL_PATH"

if [ $? -eq 0 ]; then
    echo "✅ SFTP смонтирован в $LOCAL_PATH"
    ls -la "$LOCAL_PATH"
else
    echo "❌ Ошибка монтирования"
    exit 1
fi

```

**Для 192.168.87.179 (runelfrontdev):**

```bash
# ==========================================
# НА 192.168.87.179 (runelfrontdev)
# ==========================================

micro ~/scripts/mount-sftp.sh
```

**Содержимое скрипта:**

```bash
#!/bin/bash
# ==========================================
# Скрипт для монтирования SFTP через sshfs
# С правами для пользователя dialog-llm (UID=999, GID=996)
# ==========================================

REMOTE_USER="sftp-frontdev"
REMOTE_HOST="192.168.87.99"
REMOTE_PATH="/sftp-share-dir"
LOCAL_PATH="/opt/runtel/robot/sftp-share-dir"

# UID и GID пользователя dialog-llm
USER_UID=999
USER_GID=996

# Создаем локальную папку если не существует
mkdir -p "$LOCAL_PATH"

# Проверяем, смонтировано ли уже
if mount | grep -q "$LOCAL_PATH"; then
    echo "Папка уже смонтирована. Размонтируем..."
    fusermount -u "$LOCAL_PATH" 2>/dev/null || umount "$LOCAL_PATH"
    sleep 2
fi

# Монтируем с правами dialog-llm
echo "Монтируем SFTP папку с правами dialog-llm (UID=999, GID=996)..."
sshfs -o allow_other,reconnect,follow_symlinks,uid=$USER_UID,gid=$USER_GID \
      -o IdentityFile=~/.ssh/id_rsa \
      "$REMOTE_USER@$REMOTE_HOST:$REMOTE_PATH" "$LOCAL_PATH"

if [ $? -eq 0 ]; then
    echo "✅ SFTP смонтирован в $LOCAL_PATH"
    echo "Владелец: $(ls -ld $LOCAL_PATH | awk '{print $3":"$4}')"
    ls -la "$LOCAL_PATH"
else
    echo "❌ Ошибка монтирования"
    exit 1
fi
```

```bash
chmod +x ~/scripts/mount-sftp.sh
~/scripts/mount-sftp.sh
```

### 6.4. Ручное монтирование

```bash
# ==========================================
# НА КЛИЕНТЕ (192.168.87.118 - ai2)
# ==========================================

# Монтируем с правами dialog-llm (UID=999, GID=996)
sshfs -o allow_other,reconnect,follow_symlinks,uid=999,gid=996 \
      -o IdentityFile=~/.ssh/id_rsa \
      sftp-ai2@192.168.87.99:/sftp-share-dir \
      /opt/runtel/robot/sftp-share-dir

# Проверяем, что смонтировалось
mount | grep sftp
# Должны увидеть: sshfs on /opt/runtel/robot/sftp-share-dir

# Проверяем владельца
ls -la /opt/runtel/robot/sftp-share-dir/
# Должно быть: drwxr-xr-x 1 dialog-llm dialog-llm ...

# Проверяем содержимое
ls -la /opt/runtel/robot/sftp-share-dir/
```

**Почему uid=999,gid=996?**

```bash
# Проверяем UID и GID пользователя dialog-llm
id dialog-llm
# Вывод: uid=999(dialog-llm) gid=996(dialog-llm)
```

### 6.5. Размонтирование

```bash
# ==========================================
# РАЗМОНТИРОВАНИЕ
# ==========================================

# Размонтировать (отключить) папку
fusermount -u /opt/runtel/robot/sftp-share-dir

# Или
umount /opt/runtel/robot/sftp-share-dir
```

### 6.6. Ручное перемонтирование после перезагрузки

```bash
# ==========================================
# НА 192.168.87.118 (ai2)
# ==========================================

# Способ 1: Запустить скрипт
~/scripts/mount-sftp.sh

# Способ 2: Вручную одной командой
sshfs -o allow_other,reconnect,follow_symlinks,uid=999,gid=996 \
      -o IdentityFile=~/.ssh/id_rsa \
      sftp-ai2@192.168.87.99:/sftp-share-dir \
      /opt/runtel/robot/sftp-share-dir

# Способ 3: Размонтировать и смонтировать заново
fusermount -u /opt/runtel/robot/sftp-share-dir 2>/dev/null
sshfs -o allow_other,reconnect,follow_symlinks,uid=999,gid=996 \
      -o IdentityFile=~/.ssh/id_rsa \
      sftp-ai2@192.168.87.99:/sftp-share-dir \
      /opt/runtel/robot/sftp-share-dir
```

```bash
# ==========================================
# НА 192.168.87.179 (runelfrontdev)
# ==========================================

# Способ 1: Запустить скрипт
~/scripts/mount-sftp.sh

# Способ 2: Вручную одной командой
sshfs -o allow_other,reconnect,follow_symlinks,uid=999,gid=996 \
      -o IdentityFile=~/.ssh/id_rsa \
      sftp-frontdev@192.168.87.99:/sftp-share-dir \
      /opt/runtel/robot/sftp-share-dir
```

### 6.7. Автоматическое монтирование после перезагрузки

```bash
# ==========================================
# НА 192.168.87.118 (ai2)
# ==========================================

# Редактируем crontab
crontab -e

# Добавляем строку в конец:
@reboot /root/scripts/mount-sftp.sh > /dev/null 2>&1

# Сохраняем и выходим
```

**То же самое для runelfrontdev:**

```bash
# ==========================================
# НА 192.168.87.179 (runelfrontdev)
# ==========================================

crontab -e
@reboot /root/scripts/mount-sftp.sh > /dev/null 2>&1
```

### 6.8. Алиасы для удобства

```bash
# ==========================================
# НА 192.168.87.118 (ai2) И 192.168.87.179 (runelfrontdev)
# ==========================================

cat >> ~/.bashrc << 'EOF'

# SFTP команды
alias sftp-mount='~/scripts/mount-sftp.sh'
alias sftp-umount='fusermount -u /opt/runtel/robot/sftp-share-dir 2>/dev/null'
alias sftp-status='mount | grep sftp-share-dir'
alias sftp-ls='ls -la /opt/runtel/robot/sftp-share-dir'
alias sftp-cd='cd /opt/runtel/robot/sftp-share-dir'

EOF

source ~/.bashrc
```

Теперь можно использовать:
```bash
sftp-mount    # смонтировать
sftp-umount   # размонтировать
sftp-status   # проверить статус
sftp-ls       # посмотреть содержимое
sftp-cd       # перейти в папку
```

---

## ТАБЛИЦА: КАКИЕ КЛИЕНТЫ И КАК ПОДКЛЮЧАТЬСЯ

| Клиент | Способ подключения | Особенности |
|--------|-------------------|-------------|
| **Double Commander** | Ctrl+N → SFTP → Заполнить поля | Двухпанельный, как Total Commander |
| **Dolphin** | Адресная строка: `sftp://...` | Красивый, KDE |
| **Nautilus** | Other Locations → `sftp://...` | Стандартный в Ubuntu |
| **FileZilla** | Site Manager → SFTP | Самый популярный FTP клиент |
| **SSHFS** | Монтирование через `sshfs` | Работа как с локальной папкой |
| **Konqueror** | Адресная строка: `sftp://...` | Универсальный браузер/файловый менеджер |
| **PCManFM** | Адресная строка: `sftp://...` | Легкий и быстрый |

---

## КОМАНДЫ ДЛЯ ПРОВЕРКИ НА СЕРВЕРЕ

```bash
#!/bin/bash
# ==========================================
# ВЫПОЛНЯЕТСЯ НА: backup (192.168.87.99)
# ==========================================

# Проверка прав на папки
ll /sftp-share/
# Должны увидеть:
# drwxr-xr-x 4 root root /sftp-share/
# drwxr-xr-x 5 root sftp-full-access /sftp-share/sftp-share-dir/

# Проверка прав на .ssh
ll /sftp-share/sftp-share-dir/.ssh/
# Должно быть:
# drwx------ 2 sftp-frontdev sftp-full-access .ssh/
# -rw------- 1 sftp-frontdev sftp-full-access authorized_keys

# Проверка пользователей
cat /etc/passwd | grep sftp
# Должны увидеть:
# sftp-ai2:x:1001:1001:SFTP user for AI2...
# sftp-frontdev:x:1002:1001:SFTP user for runelfrontdev...
# sftp-reader:x:1003:1002:SFTP readonly user...

# Проверка групп
cat /etc/group | grep sftp
# Должны увидеть:
# sftp-full-access:x:1001:
# sftp-readonly:x:1002:

# Проверка, что Subsystem правильный
grep "^Subsystem" /etc/ssh/sshd_config
# Должно быть: Subsystem sftp internal-sftp

# Проверка, что правила Match применились
sshd -T | grep -E "(subsystem|chrootdirectory|forcecommand)" | head -10
# Должны увидеть:
# forcecommand none
# chrootdirectory none
# subsystem sftp internal-sftp

# Проверка активных подключений
ss -tnp | grep :22

# Просмотр логов SFTP
tail -f /var/log/auth.log | grep sftp

# Проверка конфигурации SSH
sshd -t
```

---

## РЕЗЮМЕ: ГДЕ ЧТО ВЫПОЛНЯТЬ

| Шаг | Что делаем | Где выполняем |
|-----|------------|---------------|
| 1-7 | Установка и настройка сервера | **backup** (192.168.87.99) |
| 8 | Проверка/создание SSH ключей | **ai2** (192.168.87.118) и **frontdev** (192.168.87.179) |
| 9 | Добавление ключей на сервер | **backup** (192.168.87.99) |
| 10 | Проверка прав на папки | **backup** (192.168.87.99) |
| 11 | Установка графических клиентов | **ai2** и **frontdev** |
| 12 | Настройка SSHFS (опционально) | **ai2** и **frontdev** |

---

## ИТОГОВАЯ ТАБЛИЦА: ЧТО НАСТРОЕНО

| Компонент | Статус |
|-----------|--------|
| SSH сервер | ✅ Работает |
| Subsystem | ✅ internal-sftp |
| Chroot | ✅ /sftp-share |
| Группа sftp-full-access | ✅ Создана |
| Группа sftp-readonly | ✅ Создана |
| Пользователь sftp-ai2 | ✅ Создан (полный доступ) |
| Пользователь sftp-frontdev | ✅ Создан (полный доступ) |
| Пользователь sftp-reader | ✅ Создан (только чтение) |
| SSH доступ | ✅ Запрещен (nologin) |
| SSH ключи | ✅ Добавлены |
| Полный доступ для 192.168.87.118 | ✅ Работает |
| Полный доступ для 192.168.87.179 | ✅ Работает |
| Только чтение для остальных | ✅ Работает |
| Права на папки (755) | ✅ Исправлены |
| SSHFS монтирование | ✅ Настроено (опционально) |

---

**ВСЕ НАСТРОЕНО!** 🎉 

Теперь ваши пользователи могут:
1. Подключаться по SFTP **БЕЗ ПАРОЛЯ** (через SSH ключи)
2. Работать с файлами через любой SFTP клиент
3. Использовать графические программы (FileZilla, Double Commander, Nautilus)
4. Монтировать папку через SSHFS и работать как с локальной
5. Иметь разные уровни доступа (полный для доверенных машин, только чтение для остальных)

**Ключевые моменты, которые обеспечивают работу:**
- ✅ Правильная замена `Subsystem` на `internal-sftp`
- ✅ Правильные права на папки (755 вместо 775 для домашней папки)
- ✅ Правильные права на `.ssh` (700) и `authorized_keys` (600)
- ✅ Правильный владелец папки `.ssh` (пользователь, который подключается)
- ✅ Правильные права при монтировании через SSHFS (uid=999,gid=996 для dialog-llm)