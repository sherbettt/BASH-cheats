# Когда база при деплое на единичную ноду почему-то слушает сокет 5433

## Исходная проблема
Playbook падал с ошибкой:
```
unable to connect to database: подключиться к серверу через сокет "/var/lib/postgresql/patroni/.s.PGSQL.5433" не удалось: Нет такого файла или каталога
```

## Выполненные действия

### 1. Анализ текущего состояния
```bash
# Проверка работающего PostgreSQL
ss -tulpn | grep 5432
ss -tulpn | grep 5433
# Вывод: PostgreSQL слушал только на 127.0.0.1:5432

# Проверка существующих БД
sudo -u postgres psql -c "\l"
# Было только 3 системные БД: postgres, template0, template1
```

### 2. Анализ конфигурации PostgreSQL
```bash
# Проверка pg_hba.conf
cat /etc/postgresql/15/main/pg_hba.conf
# Были дублирующиеся строки для 127.0.0.1

# Проверка postgresql.conf
cat /etc/postgresql/15/main/postgresql.conf
# listen_addresses = '127.0.0.1'
# port = 5432
```

### 3. Исправление конфигурации pg_hba.conf
```bash
# Убрали дублирующиеся строки и добавили нужные IP-адреса
nano /etc/postgresql/15/main/pg_hba.conf

# Итоговый вид:
local   all             postgres                                peer
local   all             all                                     peer
host    all             all             127.0.0.1/32            md5
host    all             all             192.168.5.199/32        md5
host    all             all             10.252.1.100/32         md5
host    all             all             ::1/128                 md5
local   replication     all                                     peer
host    replication     all             127.0.0.1/32            md5
host    replication     all             ::1/128                 md5
```

### 4. Настройка подключения к внешним интерфейсам
```bash
# Редактирование postgresql.conf
nano /etc/postgresql/15/main/postgresql.conf

# Изменения:
listen_addresses = 'localhost,192.168.5.199,10.252.1.100'
```

### 5. Создание структуры директорий для Ansible
```bash
# Создание директории, которую ожидал playbook
mkdir -p /var/lib/postgresql/patroni/
chown postgres:postgres /var/lib/postgresql/patroni/
```

### 6. Создание символических ссылок для socket'ов
```bash
# Удалим симлинки и создадим заново с правильными владельцами
rm -f /var/run/postgresql/.s.PGSQL.5433
rm -f /var/lib/postgresql/patroni/.s.PGSQL.5433

# Создадим заново от пользователя postgres
sudo -u postgres ln -sf /var/run/postgresql/.s.PGSQL.5432 /var/run/postgresql/.s.PGSQL.5433
sudo -u postgres ln -sf /var/run/postgresql/.s.PGSQL.5432 /var/lib/postgresql/patroni/.s.PGSQL.5433
sudo -u postgres ln -sf /var/run/postgresql/.s.PGSQL.5432 /var/lib/postgresql/patroni/.s.PGSQL.5432

# Проверим
ls -la /var/run/postgresql/.s.PGSQL.5433
ls -la /var/lib/postgresql/patroni/.s.PGSQL.5433
```

### 7. Настройка аутентификации .pgpass
```bash
# Создание .pgpass для пользователя postgres
sudo -u postgres cat > /var/lib/postgresql/.pgpass << 'EOF'
localhost:5432:*:postgres:Aidad0Hahghooyeew3
127.0.0.1:5432:*:postgres:Aidad0Hahghooyeew3
192.168.5.199:5432:*:postgres:Aidad0Hahghooyeew3
10.252.1.100:5432:*:postgres:Aidad0Hahghooyeew3
/var/lib/postgresql/patroni:5432:*:postgres:Aidad0Hahghooyeew3
/var/lib/postgresql/patroni:5433:*:postgres:Aidad0Hahghooyeew3
EOF

# Установка правильных прав
chmod 600 /var/lib/postgresql/.pgpass
chown postgres:postgres /var/lib/postgresql/.pgpass
```

### 8. Перезапуск и проверка PostgreSQL
```bash
# Перезапуск PostgreSQL
systemctl restart postgresql

# Проверка статуса
systemctl status postgresql@15-main.service

# Проверка подключения через оба пути
sudo -u postgres psql -h /var/lib/postgresql/patroni -p 5432 -c "SELECT version();"
sudo -u postgres psql -h /var/lib/postgresql/patroni -p 5433 -c "SELECT version();"
```

### 9. Проверить связанность
```bash
sudo -u postgres psql -d rt_pbx_v2 -c "SELECT id, login, name, password FROM \"user\" WHERE login = 'root@runtel.ru';"
sudo -u postgres psql -d rt_pbx_v2 -c "\du+"
sudo -u postgres psql -d rt_pbx_v2 -c "\dt+" | ccat

root@faeton24 /etc/nginx
13:48:13 # sudo -u postgres psql -d rt_pbx_v2 -c "SELECT du.id, d.name as domain, u.login, u.name FROM domain_user du JOIN domain d ON du.domain_id = d.id JOIN \"user\" u ON du.user_id = u.id;"
 id |  domain  |     login      |          name          
----+----------+----------------+------------------------
  1 | faeton24 | root@runtel.ru | Provider administrator
(1 строка)

root@faeton24 /etc/nginx
13:49:47 # sudo -u postgres psql -d rt_pbx_v2 -c "SELECT id, name, status FROM domain;"
 id |   name   | status 
----+----------+--------
  1 | faeton24 |      1
(1 строка)

# Проверим пароль в связке domain_user
13:53:28 # sudo -u postgres psql -d rt_pbx_v2 -c "SELECT id, domain_id, user_id, uid, password FROM domain_user WHERE id = 1;"
 id | domain_id | user_id | uid |   password    
----+-----------+---------+-----+---------------
  1 |         1 |       1 | 100 | Bazei1eH8xiej
(1 строка)

##---опционально
# Обновим пароль в domain_user
sudo -u postgres psql -d rt_pbx_v2 -c "UPDATE domain_user SET password = 'Bazei1eH8xiej' WHERE id = 1;"

# Проверим
sudo -u postgres psql -d rt_pbx_v2 -c "SELECT id, domain_id, user_id, uid, password FROM domain_user WHERE id = 1;"
```

### 9. Ошибочная попытка настройки двух портов
```bash
# НЕПРАВИЛЬНО: попытка добавить второй порт через запятую
# В postgresql.conf: port = 5432,5433
# Результат: PostgreSQL не запустился с ошибкой синтаксиса

# ВОССТАНОВЛЕНИЕ: вернули обратно port = 5432
```

## Итоговое состояние
- **PostgreSQL слушает** на: 127.0.0.1:5432, 192.168.5.199:5432, 10.252.1.100:5432
- **Созданы символические ссылки** для обхода проблемы с путями Ansible
- **Настроена аутентификация** через .pgpass
- **Созданы пользователь и БД** для PBX системы

## Ключевое решение
**Символическая ссылка** `/var/lib/postgresql/patroni/.s.PGSQL.5433` → `/var/run/postgresql/.s.PGSQL.5432` позволила обойти проблему, когда Ansible ожидал подключение на порту 5433 через специфический путь, но PostgreSQL работал на порту 5432 в стандартной директории.

После этих действий playbook успешно создал пользователя `rt_pbx` и все необходимые базы данных для PBX системы.

-----
<br/>

# проблема имени хоста и времени

### 1. Проблема с временными зонами
**Проблема**: Конфликт между `/etc/timezone` (US/Eastern) и `/etc/localtime` (Europe/Moscow)

**Решение**:
```bash
# Исправили временную зону
echo "Europe/Moscow" > /etc/timezone
# /etc/localtime уже был правильным → Europe/Moscow
```

### 2. Проблема с hostname
**Проблема**: Несоответствие hostname в разных местах (был debian, стал faeton24)

**Решение**:
```bash
# Исправили /etc/hosts
sed -i 's/debian\.faeton.*debian/faeton24   faeton24/' /etc/hosts

# /etc/hostname уже был правильным: faeton24
```

### 3. Очистка БД для чистой установки
**Причина**: Для гарантированно чистой установки PBX системы

**Действия**:
```bash
# Остановили сервис
systemctl stop runtel-iface-v2.service

# Удалили все базы данных
sudo -u postgres psql -c "DROP DATABASE rt_pbx_v2;"
sudo -u postgres psql -c "DROP DATABASE rt_pbx_v2_stat;"
sudo -u postgres psql -c "DROP DATABASE rt_pbx_v2_media;"
sudo -u postgres psql -c "DROP DATABASE rt_pbx_v2_logging;"

# Удалили пользователя БД
sudo -u postgres psql -c "DROP USER rt_pbx;"
```

После заврешения плейбука првоерить:
```bash
# 1. Проверим логи system_init
journalctl -u runtel-iface-v2 | grep -i "system_init"

# 2. Проверим пользователя в БД
sudo -u postgres psql -d rt_pbx_v2 -c "SELECT id, login, password FROM \"user\";"

# 3. Проверим доступность API
curl -k http://127.0.0.1:4810/api/health
```

**Теперь playbook должен успешно установить PBX систему с нуля, без каких-либо ошибок!** Все препятствия устранены.

------
<br/>


## 📊 Статус:
- ✅ **Внешний IP пингуется** - сервер доступен
- ❌ **Порт 443 не отвечает** - проблема с пробросом портов

## Проблема:
**Порт 443 не проброшен** на роутере/firewall к серверу `192.168.5.199`

## Решения:

### 1. Настроить проброс портов на роутере:
- **Внешний порт 443** → **Внутренний 192.168.5.199:443**
- **Внешний порт 80** → **Внутренний 192.168.5.199:80**

### 2. Использовать WireGuard VPN (рекомендую):
Подключитесь к VPN сети `10.252.1.x` и используйте:
```
https://10.252.1.100
```

### 3. Временное решение - SSH туннель:
```bash
# С ноутбука создаем туннель
ssh -L 8443:192.168.5.199:443 root@185.114.136.118 -p 8089

# Теперь доступно по:
https://localhost:8443
```

### 4. Проверить настройки firewall на PBX:
```bash
# На PBX проверим iptables
iptables -L

# Или ufw если используется
ufw status
```

## Самый быстрый вариант - SSH туннель:
```bash
# Выполните на ноутбуке:
ssh -L 8443:192.168.5.199:443 root@185.114.136.118 -p 8089

# Для доступа к нескольким портам (80, 443, 22 на PBX)
ssh -L 8080:192.168.5.199:80 -L 8443:192.168.5.199:443 -L 8022:192.168.5.199:22 root@185.114.136.118 -p 8089

# Откройте браузер:
https://localhost:8443
```

**Какой вариант предпочитаете?** SSH туннель - самое быстрое временное решение.
