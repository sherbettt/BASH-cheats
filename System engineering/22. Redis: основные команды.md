**Справочник по командам Redis**

##  **Для безопасности** лучше использовать переменные окружения:
```bash
export REDISCLI_AUTH="RedisComplexPass"
# Альтернатива с --pass
redis-cli -h 127.0.0.1 -p 6380 --pass "RedisComplexPass"
# Или через URI
redis-cli -u redis://default:RedisComplexPass@127.0.0.1:6380
```

##  **Проверка пинга и подключения**
```bash
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" ping
redis-cli -h 127.0.0.1 -p 6380 AUTH "RedisComplexPass"
# Проверка с ожиданием
redis-cli -h 127.0.0.1 -p 6380 --no-auth-warning -a "RedisComplexPass" --raw ping
# Проверка с таймаутом
timeout 5 redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" ping
```

##  **Информационные и диагностические команды**

```bash
# Основная информация о сервере
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" INFO

# Конкретные разделы INFO
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" INFO server
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" INFO memory
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" INFO clients
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" INFO stats
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" INFO replication
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" INFO cpu
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" INFO persistence

# Статистика в реальном времени
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" --stat
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" --stat -i 5  # с интервалом 5 секунд

# Мониторинг задержек
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" --latency
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" --latency-history
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" --latency-dist

# Проверка внутренней задержки системы
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" --intrinsic-latency 10
```

## 🔍 **Анализ данных**

```bash
# Поиск больших ключей
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" --bigkeys
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" --bigkeys -i 0.1  # с интервалом

# Поиск ключей, потребляющих много памяти
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" --memkeys
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" --memkeys-samples 1000

# Поиск "горячих" ключей (требует политики LFU)
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" --hotkeys

# Сканирование всех ключей по шаблону
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" --scan --pattern "user:*"
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" --scan --pattern "*" | wc -l  # подсчет ключей
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" --scan --pattern "*" --quoted-pattern

# Анализ использования памяти по ключам
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" MEMORY USAGE keyname
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" MEMORY STATS
```

##  **Администрирование**

```bash
# Список всех команд
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" COMMAND
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" COMMAND COUNT

# Информация о конкретной команде
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" COMMAND INFO get set
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" COMMAND DOCS get

# Мониторинг команд в реальном времени
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" MONITOR
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" MONITOR | grep -i "set\|get"  # фильтрация

# Сброс статистики
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" CONFIG RESETSTAT

# Сохранение данных на диск
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" SAVE
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" BGSAVE
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" LASTSAVE  # время последнего сохранения

# Получение конфигурации
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" CONFIG GET *
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" CONFIG GET slave*
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" CONFIG GET master*
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" CONFIG GET maxmemory*
```

##  **Работа с ключами**

```bash
# Получить все ключи (осторожно на больших БД!)
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" KEYS "*"
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" KEYS "prefix:*"

# Подсчет количества ключей
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" DBSIZE

# Получить случайный ключ
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" RANDOMKEY

# Проверить существование ключа
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" EXISTS mykey
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" EXISTS key1 key2 key3

# Узнать тип значения
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" TYPE mykey

# Операции с TTL (время жизни)
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" TTL mykey
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" PTTL mykey  # в миллисекундах
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" EXPIRE mykey 3600
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" PERSIST mykey  # убрать TTL

# Переименование и удаление
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" RENAME oldkey newkey
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" DEL mykey
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" UNLINK mykey  # асинхронное удаление
```

##  **Работа с репликацией (ваш текущий случай)**
```bash
# Проверить статус репликации
redis-cli -h 127.0.0.1 -p 6380 INFO replication
redis-cli -h 127.0.0.1 -p 6380 INFO replication | grep -E "(role|master_host|master_link_status)"

# Подробная информация о репликации
redis-cli -h 127.0.0.1 -p 6380 ROLE

# Принудительно переподключиться к мастеру
redis-cli -h 127.0.0.1 -p 6380 SLAVEOF NO ONE  # Стать мастером
redis-cli -h 127.0.0.1 -p 6380 SLAVEOF 192.168.87.66 6380  # Снова стать репликой
redis-cli -h 127.0.0.1 -p 6380 SLAVEOF "192.168.87.66" 6380  # с кавычками

# Проверить задержку репликации
redis-cli -h 127.0.0.1 -p 6380 --latency

# Информация о мастере
redis-cli -h 127.0.0.1 -p 6380 INFO replication | grep -A5 -B5 "master"

# Приостановить/возобновить репликацию
redis-cli -h 127.0.0.1 -p 6380 SLAVEOF NO ONE && sleep 10 && redis-cli -h 127.0.0.1 -p 6380 SLAVEOF 192.168.87.66 6380
```

##  **Диагностика соединения с мастером**
```bash
# Попробовать подключиться к мастеру напрямую
redis-cli -h 192.168.87.66 -p 6380 -a "RedisComplexPass" PING
redis-cli -h 192.168.87.66 -p 6380 -a "RedisComplexPass" INFO replication

# Проверить, слушает ли мастер на порту 6380
nc -zv 192.168.87.66 6380
timeout 5 telnet 192.168.87.66 6380
ss -tlnp | grep 6380  # на мастере

# Посмотреть логи Redis для диагностики
tail -f /var/log/redis/redis-server.log
tail -100 /var/log/redis/redis-server.log | grep -i "repl\|master\|slave\|error"
journalctl -u redis-server -f  # если используется systemd

# Проверить сетевые соединения
ss -tn | grep 6380
netstat -tlnp | grep 6380
```

##  **Кластерные команды (если будете настраивать кластер)**

```bash
# Информация о кластере (когда включен)
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" --cluster info 127.0.0.1:6380
redis-cli -h 127.0.0.1 -p 6380 --cluster nodes 127.0.0.1:6380

# Проверка состояния кластера
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" --cluster check 127.0.0.1:6380
redis-cli -h 127.0.0.1 -p 6380 --cluster call 127.0.0.1:6380 INFO

# Узлы кластера
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" CLUSTER NODES
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" CLUSTER INFO
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" CLUSTER SLOTS
```

##  **Безопасность и ACL**

```bash
# Список пользователей ACL
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" ACL LIST
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" ACL USERS

# Информация о текущем пользователе
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" ACL WHOAMI
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" ACL GETUSER default

# Смена пароля
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" CONFIG SET requirepass "NewComplexPass"

# Проверка прав
redis-cli -h 127.0.0.1 -p 6380 --user default --pass "RedisComplexPass" ACL WHOAMI
```

##  **Практические примеры**

```bash
# Экспорт дампа RDB
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" --rdb dump.rdb
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" --rdb /tmp/backup-$(date +%Y%m%d).rdb

# Выполнение Lua скрипта
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" --eval script.lua key1 key2 , arg1 arg2
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" EVAL "return redis.call('INFO')" 0

# Многократное выполнение команды
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" -r 100 -i 1 GET counter
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" -r 1000 -i 0.01 INCR counter

# Пакетная обработка
echo -e "SET key1 value1\nGET key1\nDEL key1" | redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" --pipe

# Резервное копирование через pipeline
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" --scan --pattern "*" | while read key; do
    redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" --raw DUMP "$key" > "backup-$key.bin"
done
```

##  **Специальные команды для вашего случая (master-slave с проблемами)**

```bash
# Проверить почему мастер недоступен
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" INFO replication | grep -E "(master_link_status|master_last_io_seconds_ago|master_link_down_since_seconds)"

# Временно отключить репликацию для диагностики
redis-cli -h 127.0.0.1 -p 6380 SLAVEOF NO ONE
redis-cli -h 127.0.0.1 -p 6380 INFO replication  # проверяем что стали мастером
redis-cli -h 127.0.0.1 -p 6380 SLAVEOF 192.168.87.66 6380  # возвращаем

# Проверить настройки брандмауэра
iptables -L -n | grep 6380
ufw status | grep 6380

# Проверить конфигурацию репликации в файле
grep -i "slave\|replica\|master" /etc/redis/redis.conf
```

##  **Мониторинг производительности**

```bash
# Непрерывный мониторинг
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" --stat -i 1

# Бенчмарк
redis-benchmark -h 127.0.0.1 -p 6380 -a "RedisComplexPass" -t set,get -n 100000 -c 50

# Тест LRU
redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" --lru-test 1000000
```

##  **Аварийное восстановление**

```bash
# Если репликация сломана и нужно восстановить данные
redis-cli -h 127.0.0.1 -p 6380 SAVE  # сохраняем текущие данные
scp /var/lib/redis/dump.rdb user@192.168.87.66:/var/lib/redis/  # копируем на мастер
# На мастере: остановить Redis, заменить dump.rdb, запустить Redis

# Экстренное повышение реплики до мастера
redis-cli -h 127.0.0.1 -p 6380 SLAVEOF NO ONE
redis-cli -h 127.0.0.1 -p 6380 CONFIG SET slave-read-only no
```

##  **Создание алиасов для удобства**

```bash
# Добавить в ~/.bashrc или ~/.bash_profile
alias redis-local='redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass"'
alias redis-master='redis-cli -h 192.168.87.66 -p 6380 -a "RedisComplexPass"'
alias redis-info='redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" INFO'
alias redis-repl='redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" INFO replication'
alias redis-monitor='redis-cli -h 127.0.0.1 -p 6380 -a "RedisComplexPass" --stat'
```

-----------------------------


