# Установка Nextcloud 33 на Ubuntu 24.04 с Nginx и PostgreSQL + OnlyOffice + SSO Keycloack

## Предисловие

Ubuntu 24.04 (Noble Numbat) использует стабильные версии пакетов. Здесь **PHP 8.3**, а не 8.4.  
Инструкция построена на связке **Nginx + PostgreSQL** – производительной и надёжной.  
В конце добавим **OnlyOffice** для онлайн-редактирования документов прямо в браузере.

## Что потребуется

- Сервер с Ubuntu 24.04 LTS
- Nextcloud сервер: https://download.nextcloud.com/server/releases/
- Домен (или поддомен) для OnlyOffice, например `only-office.runtel.ru`
- (опционально) https://github.com/nextcloud/all-in-one/releases/tag/v12.9.2

---

## Шаг 1. Установка необходимых пакетов

```bash
sudo apt update
sudo apt upgrade -y
sudo apt install -y nginx postgresql postgresql-contrib \
  php8.3-fpm php8.3-pgsql \
  php8.3-{zip,xml,gd,mbstring,curl,intl,bcmath,redis,imagick} \
  unzip wget certbot python3-certbot-nginx
```

## Шаг 2. Настройка PHP

```bash
sudo sed -i 's/upload_max_filesize = 2M/upload_max_filesize = 10G/' /etc/php/8.3/fpm/php.ini
sudo sed -i 's/post_max_size = 8M/post_max_size = 10G/' /etc/php/8.3/fpm/php.ini
sudo sed -i 's/memory_limit = 128M/memory_limit = 512M/' /etc/php/8.3/fpm/php.ini
sudo sed -i 's/;date.timezone =/date.timezone = UTC/' /etc/php/8.3/fpm/php.ini

sudo systemctl restart php8.3-fpm
```

## Шаг 3. Скачивание и распаковка Nextcloud

```bash
cd /tmp
wget https://download.nextcloud.com/server/releases/latest.zip
unzip latest.zip
sudo mv nextcloud /var/www/html/
sudo chown -R www-data:www-data /var/www/html/nextcloud
```

## Шаг 4. Настройка базы данных PostgreSQL

```bash
sudo -u postgres psql
```

```sql
CREATE DATABASE nextcloud;
CREATE USER nextcloud_user WITH PASSWORD 'ваш_пароль';
GRANT ALL PRIVILEGES ON DATABASE nextcloud TO nextcloud_user;
ALTER DATABASE nextcloud OWNER TO nextcloud_user;
\q
```

## Шаг 5. Полный рабочий конфиг Nginx

См. пример https://docs.nextcloud.com/server/19/admin_manual/installation/nginx.html

Итоговый файл `/etc/nginx/sites-available/nextcloud`:

```nginx
# ============================================================
# Конфигурация Nginx для Nextcloud 33
# Ubuntu 24.04 | PHP 8.3 FPM | PostgreSQL
# ============================================================

upstream php-handler {
    server unix:/var/run/php/php8.3-fpm.sock;
}

# ------------------------------------------------------------
# Перенаправление HTTP на HTTPS
# ------------------------------------------------------------
server {
    listen 80;
    listen [::]:80;
    server_name nextcloud.runtel.ru;
    return 301 https://$server_name$request_uri;
}

# ------------------------------------------------------------
# Основной блок HTTPS
# ------------------------------------------------------------
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    
    server_name nextcloud.runtel.ru;
    root /var/www/html/nextcloud;

    # SSL сертификаты (настройте под себя)
    ssl_certificate /etc/nginx/runtel.pem;
    ssl_certificate_key /etc/nginx/runtel.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;

    # Безопасные заголовки
    add_header Referrer-Policy "no-referrer" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Download-Options "noopen" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Permitted-Cross-Domain-Policies "none" always;
    add_header X-Robots-Tag "none" always;
    add_header X-XSS-Protection "1; mode=block" always;

    fastcgi_hide_header X-Powered-By;
    client_max_body_size 10G;
    fastcgi_buffers 64 4K;

    # Gzip сжатие
    gzip on;
    gzip_vary on;
    gzip_comp_level 4;
    gzip_min_length 256;
    gzip_proxied expired no-cache no-store private no_last_modified no_etag auth;
    gzip_types application/atom+xml application/javascript application/json 
               application/ld+json application/manifest+json application/rss+xml 
               application/vnd.geo+json application/vnd.ms-fontobject 
               application/x-font-ttf application/x-web-app-manifest+json 
               application/xhtml+xml application/xml font/opentype image/bmp 
               image/svg+xml image/x-icon text/cache-manifest text/css text/plain 
               text/vcard text/vnd.rim.location.xloc text/vtt text/x-component 
               text/x-cross-domain-policy;

    # CalDAV / CardDAV
    location = /.well-known/carddav {
        return 301 $scheme://$host/remote.php/dav;
    }
    location = /.well-known/caldav {
        return 301 $scheme://$host/remote.php/dav;
    }

    # Основной location
    location / {
        rewrite ^ /index.php;
    }

    # Запрет доступа к служебным папкам
    location ~ ^\/(?:build|tests|config|lib|3rdparty|templates|data)\/ {
        deny all;
    }
    location ~ ^\/(?:\.|autotest|occ|issue|indie|db_|console) {
        deny all;
    }

    # Обработка PHP
    location ~ ^\/(?:index|remote|public|cron|core\/ajax\/update|status|ocs\/v[12]|updater\/.+|ocs-provider\/.+)(?:\.php)(?:$|\/) {
        fastcgi_split_path_info ^(.+?\.php)(\/.*|)$;
        set $path_info $fastcgi_path_info;
        try_files $fastcgi_script_name =404;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param PATH_INFO $path_info;
        fastcgi_param HTTPS on;
        fastcgi_param modHeadersAvailable true;
        fastcgi_param front_controller_active true;
        fastcgi_pass php-handler;
        fastcgi_intercept_errors on;
        fastcgi_request_buffering off;
    }

    # Статические файлы с кешированием
    location ~ \.(?:css|js|woff2?|svg|gif|map)$ {
        try_files $uri /index.php$request_uri;
        add_header Cache-Control "public, max-age=15778463";
        access_log off;
    }

    # Изображения и медиа
    location ~ \.(?:png|html|ttf|ico|jpg|jpeg|bcmap|mp4|webm)$ {
        try_files $uri /index.php$request_uri;
        access_log off;
    }
}
```

## Шаг 6. Активация конфигурации

```bash
sudo ln -sf /etc/nginx/sites-available/nextcloud /etc/nginx/sites-enabled/
sudo rm -f /etc/nginx/sites-enabled/default
sudo nginx -t
sudo systemctl restart nginx php8.3-fpm
```

## Шаг 7. Настройка сертификатов Let's Encrypt

```bash
sudo certbot --nginx -d nextcloud.runtel.ru
```

Для тестового окружения можно использовать самоподписанный сертификат:

```bash
sudo mkdir -p /etc/nginx/ssl
sudo openssl req -x509 -nodes -days 365 -newkey rsa:2048 \
  -keyout /etc/nginx/ssl/nextcloud.key \
  -out /etc/nginx/ssl/nextcloud.crt \
  -subj "/CN=nextcloud.runtel.ru"
```

## Шаг 8. Настройка прав для Nextcloud

```bash
cd /var/www/html/nextcloud
sudo chown -R www-data:www-data .
sudo find . -type d -exec chmod 755 {} \;
sudo find . -type f -exec chmod 644 {} \;
sudo chmod +x occ
sudo chmod 750 config
```

## Шаг 9. Настройка cron

```bash
echo '*/5 * * * * www-data php -f /var/www/html/nextcloud/cron.php' | sudo tee /etc/cron.d/nextcloud
sudo chmod 644 /etc/cron.d/nextcloud
```

## Шаг 10. Завершение установки через веб-интерфейс

Откройте `https://nextcloud.runtel.ru`

**Параметры установки:**

| Поле | Значение |
|------|----------|
| Имя администратора | (придумать) |
| Пароль администратора | (надёжный) |
| Папка данных | `/var/www/html/nextcloud/data` |
| База данных | PostgreSQL |
| Пользователь БД | `nextcloud_user` |
| Пароль БД | (тот, что задали) |
| Имя БД | `nextcloud` |
| Хост | `127.0.0.1` |
| Порт | `5432` |

## Шаг 11. Пост-установочные действия

```bash
sudo -u www-data php /var/www/html/nextcloud/occ status
sudo -u www-data php /var/www/html/nextcloud/occ db:add-missing-indices
sudo -u www-data php /var/www/html/nextcloud/occ background:cron
```

---

## Шаг 12. Установка и настройка OnlyOffice для онлайн-редактирования

Этот шаг позволит открывать и редактировать документы (DOCX, XLSX, PPTX) прямо в браузере, прямо как в Google Docs.

### 12.1. Установка зависимостей для OnlyOffice

```bash
sudo apt update
sudo apt install -y postgresql rabbitmq-server
```

### 12.2.1 Создание базы данных для OnlyOffice

```
sudo -u postgres psql
```
```sql
CREATE USER onlyoffice WITH PASSWORD 'OfficePass99rntl';
CREATE DATABASE onlyoffice OWNER onlyoffice;
GRANT ALL PRIVILEGES ON DATABASE onlyoffice TO onlyoffice;
ALTER DATABASE onlyoffice OWNER TO onlyoffice;
```
#### Если были созданы пользователи ранее неправлиьно, то удаляем
```sql
-- 2. Удаляем старых пользователей и базу, если они есть
DROP DATABASE IF EXISTS onlyoffice;
DROP USER IF EXISTS onlyoffice;
DROP USER IF EXISTS onlyoffice_user;

-- 3. Создаём пользователя (именно 'onlyoffice')
CREATE USER onlyoffice WITH PASSWORD 'OfficePass99rntl';

-- 4. Создаём базу данных (именно 'onlyoffice') и назначаем владельца
CREATE DATABASE onlyoffice OWNER onlyoffice;

-- 5. Даём все привилегии на базу данных новому пользователю
GRANT ALL PRIVILEGES ON DATABASE onlyoffice TO onlyoffice;

-- 6. Выходим из psql
\q
```

или
```bash
sudo -u postgres psql << EOF
CREATE USER onlyoffice WITH PASSWORD 'OfficePass99rntl';
CREATE DATABASE onlyoffice OWNER onlyoffice;
GRANT ALL PRIVILEGES ON DATABASE onlyoffice TO onlyoffice;
ALTER DATABASE onlyoffice OWNER TO onlyoffice;
EOF
```
иди ещё можно так
```bash
# Создаём пользователя и базу данных
sudo -i -u postgres psql -c "CREATE USER onlyoffice WITH PASSWORD 'strong_password_here';"
sudo -i -u postgres psql -c "CREATE DATABASE onlyoffice OWNER onlyoffice;"
sudo -i -u postgres psql -c "GRANT ALL PRIVILEGES ON DATABASE onlyoffice TO onlyoffice;"
sudo -i -u postgres psql -c "ALTER DATABASE onlyoffice OWNER TO onlyoffice;"

# Проверяем создание
sudo -i -u postgres psql -c "\l" | grep onlyoffice
```


### 12.2.2 Добавляем правила аутентификации в pg_hba.conf
```bash
mcedit /etc/postgresql/16/main/pg_hba.conf
```
```conf
# OnlyOffice specific rule (allow password auth)
host    onlyoffice     onlyoffice     127.0.0.1/32            md5
host    onlyoffice     onlyoffice     ::1/128                 md5
# Allow all local connections with password (для надёжности)
host    all             all             127.0.0.1/32            md5
host    all             all             ::1/128                 md5
```
#### Перезапускаем PostgreSQL
```bash
sudo systemctl restart postgresql
```

### 12.2.3 Создаём файл .pgpass для автоматической авторизации
```bash
echo "127.0.0.1:5432:onlyoffice:onlyoffice:OfficePass99rntl" | sudo tee /root/.pgpass
sudo chmod 600 /root/.pgpass
```

#### Проверяем подключение (должно работать без запроса пароля)
```bash
psql -h 127.0.0.1 -U onlyoffice -d onlyoffice -c "SELECT 1"
```

### 12.3. Добавление репозитория и установка OnlyOffice Document Server

```bash
# Добавляем GPG ключ
curl -fsSL https://download.onlyoffice.com/GPG-KEY-ONLYOFFICE | sudo gpg --dearmor -o /etc/apt/trusted.gpg.d/onlyoffice.gpg

# Добавляем репозиторий
echo "deb [signed-by=/etc/apt/trusted.gpg.d/onlyoffice.gpg] https://download.onlyoffice.com/repo/debian squeeze main" | sudo tee /etc/apt/sources.list.d/onlyoffice.list
```

### 12.3.1. Прямяая установка из пакета
```
# Обновляем и устанавливаем
sudo apt update
sudo apt install -y onlyoffice-documentserver
```

**В процессе установки:**
- Введите пароль для пользователя `onlyoffice` PostgreSQL (тот, что задали выше)
- Примите лицензию `ttf-mscorefonts-installer` (клавиша Tab → OK → Enter)
- Примите EULA OnlyOffice (выберите Yes)

### 12.3.2. Установка с предустанвокой в debconf-set-selections
```bash
# 1. Добавляем репозиторий OnlyOffice (уже есть, но проверим)
curl -fsSL https://download.onlyoffice.com/GPG-KEY-ONLYOFFICE | sudo gpg --dearmor -o /etc/apt/trusted.gpg.d/onlyoffice.gpg
echo "deb [signed-by=/etc/apt/trusted.gpg.d/onlyoffice.gpg] https://download.onlyoffice.com/repo/debian squeeze main" | sudo tee /etc/apt/sources.list.d/onlyoffice.list

# 2. Предустанавливаем пароль в debconf (это ВАЖНО!)
echo "onlyoffice-documentserver onlyoffice/db/password password OfficePass99rntl" | sudo debconf-set-selections
echo "onlyoffice-documentserver onlyoffice/db/user string onlyoffice" | sudo debconf-set-selections
echo "onlyoffice-documentserver onlyoffice/db/dbname string onlyoffice" | sudo debconf-set-selections
echo "onlyoffice-documentserver onlyoffice/db/host string 127.0.0.1" | sudo debconf-set-selections
echo "onlyoffice-documentserver onlyoffice/db/port string 5432" | sudo debconf-set-selections

# 3. Обновляем список пакетов
sudo apt update

# 4. Устанавливаем OnlyOffice
sudo apt install -y onlyoffice-documentserver
```

### 12.4. Настройка Nginx для OnlyOffice

Встроенный Nginx OnlyOffice не используется, и вы должны проксировать запросы через ваш основной Nginx.

Создайте конфиг `/etc/nginx/sites-available/onlyoffice`:

```nginx
server {
    listen 80;
    server_name only-office.runtel.ru;
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name only-office.runtel.ru;

    ssl_certificate /etc/nginx/runtel.pem;
    ssl_certificate_key /etc/nginx/runtel.pem;
    ssl_protocols TLSv1.2 TLSv1.3;

    # ПРАВИЛО ДЛЯ СТАТИКИ: убираем версию из URL
#    location ~ ^/\d+\.\d+\.\d+-[a-f0-9]+/(.*)$ {
#        proxy_pass http://127.0.0.1:8000/$1;
#        proxy_http_version 1.1;
#        proxy_set_header Host $host;
#        proxy_set_header X-Real-IP $remote_addr;
#        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#        proxy_set_header X-Forwarded-Proto $scheme;
#       proxy_set_header Upgrade $http_upgrade;
#       proxy_set_header Connection "upgrade";
#        expires 30d;
#        add_header Cache-Control "public, immutable";
#    }

    location ~ ^/([\d]+\.[\d]+\.[\d]+[\.|-][\w]+)/(?<path>.*)$ {
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_pass http://127.0.0.1:8000/$path$is_args$args;
        proxy_http_version 1.1;
    }


    # Прокси для всех остальных запросов
    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        client_max_body_size 100M;
        proxy_read_timeout 3600s;
        proxy_connect_timeout 3600s;
        proxy_send_timeout 3600s;
        proxy_buffering off;
    }
}
```

Активируйте конфиг:

```bash
sudo ln -sf /etc/nginx/sites-available/onlyoffice /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx
```

### 12.5. Настройка JWT (безопасность)

***Если установка OnlyOffice прошла с предыдущими шагами верно, то JWT секрет автоматически подставился.*** 
Однако, **узнать его необходимо** для дальнейших шагов ниже. 

#### Узнать JWT секрет (обязательно!)
```bash
# Смотрим сгенерированный JWT секрет
sudo documentserver-jwt-status.sh
```
```
Your JWT settings:
JWT enabled -  true
JWT secret  -  <JWT-secret>
JWT header  -  Authorization
```

#### НЕВЕРОЯТНО ВАЖНО!
Подставить данный JWT токен нужно будет в настройках самого NextCloud для OnlyOffice: https://nextcloud.runtel.ru/settings/admin/onlyoffice


#### Отредактируйте файл `/etc/onlyoffice/documentserver/local.json`:
```bash
sudo mcedit /etc/onlyoffice/documentserver/local.json
```

Найдите секцию `"token"` и приведите к виду:
```json
"token": {
    "enable": {
        "request": {
            "inbox": true,
            "outbox": true
        },
        "browser": true
    },
    "inbox": {
        "header": "Authorization",
        "inBody": false
    },
    "outbox": {
        "header": "Authorization",
        "inBody": false
    },
    "secret": {
        "inbox": {
            "string": "ВАШ_СЕКРЕТНЫЙ_КЛЮЧ"
        },
        "outbox": {
            "string": "ВАШ_СЕКРЕТНЫЙ_КЛЮЧ"
        },
        "browser": {
            "string": "ВАШ_СЕКРЕТНЫЙ_КЛЮЧ"
        }
    }
}
```
Замените `ВАШ_СЕКРЕТНЫЙ_КЛЮЧ` на надёжный пароль (сгенерируйте: `openssl rand -base64 32`).

#### Перезапустите OnlyOffice:
```bash
sudo systemctl restart onlyoffice-documentserver
```

### 12.6. Установка и настройка приложения OnlyOffice в Nextcloud
```bash
# 1. Устанавливаем приложение OnlyOffice (если ещё не установлено)
sudo -u www-data php /var/www/html/nextcloud/occ app:install onlyoffice

# 2. Настраиваем адрес сервера документов
sudo -u www-data php /var/www/html/nextcloud/occ config:app:set onlyoffice DocumentServerUrl --value="https://only-office.runtel.ru/"

# 3. Указываем JWT секрет (тот, что сгенерировался автоматически)
sudo -u www-data php /var/www/html/nextcloud/occ config:app:set onlyoffice jwt_secret --value="<JWT-secret>"

# 4. Отключаем проверку SSL (для самоподписанных сертификатов)
sudo -u www-data php /var/www/html/nextcloud/occ config:app:set onlyoffice verify_peer_off --value="true"

# 5. Проверяем соединение
sudo -u www-data php /var/www/html/nextcloud/occ onlyoffice:check

# 6. Проверяем установку приложения через список
sudo -u www-data php /var/www/html/nextcloud/occ app:list | grep onlyoffice
```

#### Если команда onlyoffice:check не найдена
Возможно, приложение только что установлено и нужно обновить список команд:
```bash
# Очищаем кеш Nextcloud
sudo -u www-data php /var/www/html/nextcloud/occ app:update --all

# Или просто проверяем статус
sudo -u www-data php /var/www/html/nextcloud/occ app:list | grep onlyoffice
```


### 12.7. Проверка работы OnlyOffice
```bash
# Проверка соединения из Nextcloud
sudo -u www-data php /var/www/html/nextcloud/occ onlyoffice:check

# Откройте в браузере проверочную страницу OnlyOffice
# https://only-office.runtel.ru/welcome/

# Проверяем, что OnlyOffice отвечает
curl -k https://only-office.runtel.ru/welcome/

# Проверяем логи OnlyOffice
sudo journalctl -u onlyoffice-documentserver -n 50 --no-pager

# Проверяем логи Nginx
sudo tail -f /var/log/nginx/error.log
```

### 12.8. Проверка портов
ВАЖНО:
- ***`docservice`*** (основной сервис OnlyOffice) слушает на порту 8000. Это Python/Node.js процесс, который обрабатывает запросы на редактирование документов.
- Встроенный Nginx OnlyOffice (ds.conf) настроен на порт 80, но его сервис (nginx-ds) не запущен и даже не существует в systemd. Поэтому ваш внешний Nginx должен проксировать запросы напрямую на docservice:8000.

```bash
ss -tlnp | grep -E ":(80|443|8080|8000|3000)" | grep -v nginx
ss -tlnp | grep -E ":(80|443|8080|8000|3000)"
grep -r "listen" /etc/onlyoffice/documentserver/nginx/

sudo ss -tlnp | grep 8000
# Показал: LISTEN *:8000 - users:(("docservice",pid=4188,fd=24))
```

#### Проверяем напрямую и через Nginx
```bash
sudo nginx -t
sudo systemctl restart nginx
```
```bash
# Прямой доступ к docservice (должен работать)
curl http://127.0.0.1:8000/welcome/

# Доступ через ваш Nginx (должен работать)
curl -k https://only-office.runtel.ru/welcome/

# Смотрим заголовки ответа
curl -I -k https://only-office.runtel.ru/welcome/
```


### 12.9. Установка MIME типов
Скорее всего MIME типы по умолчанию у вас не установлены. Они нужны именно для того, чтобы открывать документы прямо в браузере.
```bash
# Проверяем, что Nextcloud видит OnlyOffice
sudo -u www-data php /var/www/html/nextcloud/occ onlyoffice:documentserver

# Проверяем версию приложения
sudo -u www-data php /var/www/html/nextcloud/occ app:list | grep onlyoffice

# Проверяем настройки OnlyOffice в Nextcloud
sudo -u www-data php /var/www/html/nextcloud/occ config:app:get onlyoffice formats

# Если пусто - устанавливаем форматы
sudo -u www-data php /var/www/html/nextcloud/occ config:app:set onlyoffice formats --value='{
    "docx": {"edit": true, "view": true},
    "xlsx": {"edit": true, "view": true},
    "pptx": {"edit": true, "view": true},
    "doc": {"edit": true, "view": true},
    "xls": {"edit": true, "view": true},
    "ppt": {"edit": true, "view": true},
    "odt": {"edit": true, "view": true},
    "ods": {"edit": true, "view": true},
    "odp": {"edit": true, "view": true},
    "rtf": {"edit": true, "view": true},
    "djvu": {"edit": true, "view": true}
}'
```

#### Проверяем права на запись временных файлов
```bash
# Проверяем и исправляем права
sudo chown -R www-data:www-data /var/www/html/nextcloud/data
sudo chmod -R 755 /var/www/html/nextcloud/data
sudo chown -R www-data:www-data /tmp
```

#### Проверяем CORS и заголовки
```bash
# Проверяем заголовки ответа OnlyOffice
curl -I -k https://only-office.runtel.ru/healthcheck

# Проверяем, что OnlyOffice возвращает правильные заголовки
curl -k https://only-office.runtel.ru/healthcheck
# Должен вернуть "true"
```

#### Смотрим логи Nextcloud
```bash
# Проверяем логи Nextcloud
sudo tail -f /var/www/html/nextcloud/data/nextcloud.log | grep -i onlyoffice
```

#### Проверяем формат URL (важно!)
```bash
# Проверяем текущий URL
sudo -u www-data php /var/www/html/nextcloud/occ config:app:get onlyoffice DocumentServerUrl

# Если нет слеша в конце - добавляем
sudo -u www-data php /var/www/html/nextcloud/occ config:app:set onlyoffice DocumentServerUrl --value="https://only-office.runtel.ru/"
```

#### Проверяем, что OnlyOffice может конвертировать файлы
```bash
# Смотрим логи конвертера
sudo journalctl -u ds-converter.service -n 50 --no-pager

# Проверяем статус конвертера
sudo systemctl status ds-converter.service --no-pager
```


Теперь зайдите в Nextcloud, создайте новый документ Word, Excel или PowerPoint — он должен открыться в редакторе прямо в браузере.

---

## Шаг 13. Устранение распространённых проблем

### 13.1. Ошибка 403 Forbidden

```bash
sudo chmod 755 /var/www/html/nextcloud
sudo chmod 755 /var/www/html/nextcloud/config
sudo systemctl restart nginx php8.3-fpm
```

### 13.2. Ошибка подключения к PostgreSQL

```bash
sudo -u postgres psql -c "ALTER USER nextcloud_user WITH PASSWORD 'новый_пароль';"
```

### 13.3. Циклический редирект (HTTP → HTTP)

Убедитесь, что в конфиге Nginx есть `fastcgi_param HTTPS on;`

### 13.4. Белая страница после установки

```bash
sudo chown -R www-data:www-data /var/www/html/nextcloud
sudo chmod -R 755 /var/www/html/nextcloud
sudo systemctl restart nginx php8.3-fpm
```
Напрямую посомтреть файл
```
curl -k "https://nextcloud.runtel.ru/apps/onlyoffice/download?doc=eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJhY3Rpb24iOiJkb3dubG9hZCIsImZpbGVJZCI6NCwidXNlcklkIjoiYWRtaW4ifQ.UJMPiJqMnQNjj6BFbfWaGsxn-3QYZpKPSR60yrUu330" \
  -H "Authorization: Bearer vIvmWbYpTx4smJf3sm0thfkw0im7XTvz"
```


### 13.5. Убрать предупреждение о pq.so

```bash
sudo rm -f /etc/php/8.3/mods-available/pq.ini 2>/dev/null
sudo systemctl restart php8.3-fpm
```

### 13.6. OnlyOffice не открывает документы (JWT ошибка)

Убедитесь, что секретный ключ совпадает в `/etc/onlyoffice/documentserver/local.json` и в настройках Nextcloud:

```bash
# Проверяем, что JWT секрет установлен
sudo -u www-data php /var/www/html/nextcloud/occ config:app:get onlyoffice jwt_secret

# Если пусто - устанавливаем
sudo -u www-data php /var/www/html/nextcloud/occ config:app:set onlyoffice jwt_secret --value="vIvmWbYpTx4smJf3sm0thfkw0im7XTvz"

# Проверяем отключение проверки сертификата
sudo -u www-data php /var/www/html/nextcloud/occ config:app:get onlyoffice verify_peer_off

# Если не true - устанавливаем
sudo -u www-data php /var/www/html/nextcloud/occ config:app:set onlyoffice verify_peer_off --value="true"

# Очистка кеша
sudo -u www-data php /var/www/html/nextcloud/occ files:scan --all

# Перезапуск OnlyOffice после изменений
sudo systemctl restart onlyoffice-documentserver
sudo systemctl restart nginx
```

### 13.7. OnlyOffice показывает "Download with invalid jwt"

Добавьте в конфиг Nginx для OnlyOffice дополнительный заголовок:

```nginx
proxy_set_header AuthorizationJwt $http_authorization;
```

И перезапустите Nginx.

------------------------
<br/>


Вы абсолютно правы! Исправляю. Добавляю **полные настройки клиента в Keycloak** после его создания.

---

## Часть 1. Настройка Keycloak (Сторона Identity Provider)

### 1.1. Создание клиента в Keycloak

**Где делали:** Веб-интерфейс Keycloak (https://sso.runtel.ru:8443)

**Навигация:**
1. Зашли в Admin Console Keycloak
2. Выбрали realm `runtel`
3. Перешли в раздел **Clients**
4. Нажали **Create client**

**Параметры создания клиента (первый экран):**

| Параметр | Значение | Пояснение |
|----------|----------|-----------|
| Client ID | `nextcloud` | Уникальный идентификатор клиента |
| Client protocol | `OpenID Connect` | Протокол аутентификации |
| Root URL | `https://nextcloud.runtel.ru` | Базовый URL Nextcloud |

Нажали **Next**.

### 1.2. Детальные настройки созданного клиента

**Где делали:** Clients → nextcloud → вкладка **Settings**

После создания клиента необходимо настроить следующие параметры:

#### Основные настройки (General Settings)

| Параметр | Значение | Пояснение |
|----------|----------|-----------|
| Client ID | `nextcloud` | Уникальный идентификатор |
| Name | `nextcloud` | Отображаемое имя |
| Description | `For https://nextcloud.runtel.ru/apps/dashboard/` | Описание (необязательно) |
| Always display in UI | `OFF` | Показывать ли клиент в интерфейсе входа |

#### Настройки доступа (Access Settings)

| Параметр | Значение | Пояснение |
|----------|----------|-----------|
| Root URL | `https://nextcloud.runtel.ru` | Базовый URL (без звёздочки!) |
| Home URL | `https://nextcloud.runtel.ru/` | Домашняя страница после входа |
| **Valid redirect URIs** | `https://nextcloud.runtel.ru/apps/user_oidc/code` | **Критически важно!** URL для перенаправления после входа |
| Valid post logout redirect URIs | `https://nextcloud.runtel.ru/*` | Куда перенаправлять после выхода |
| Web origins | `https://nextcloud.runtel.ru/` | Разрешённые источники для CORS |
| Admin URL | *(оставить пустым)* | Не требуется |

**Важные замечания по настройкам:**
- `Valid redirect URIs` должен быть **точным** URL, без звёздочек
- `Root URL` и `Admin URL` не должны содержать `*` (звёздочку)
- `Valid post logout redirect URIs` может содержать `*` для разрешения всех путей

#### Настройки аутентификации (Authentication Settings)

| Параметр | Значение | Пояснение |
|----------|----------|-----------|
| Client authentication | `ON` | Включает аутентификацию клиента (генерируется Client Secret) |
| Authorization | `OFF` | Тонкая настройка прав (не требуется) |
| Standard flow | `ON` | Разрешает стандартный OIDC поток (код авторизации) |
| Implicit flow | `OFF` | Не используется |
| Direct access grants | `OFF` | Не используется |
| Service accounts roles | `OFF` | Не требуется |

**Пояснение по Client authentication:**
- При включении этой опции генерируется **Client Secret**
- Client Secret находится на вкладке **Credentials**

### 1.3. Получение Client Secret

**Где делали:** Clients → nextcloud → вкладка **Credentials**

**Что получили:** 
```
Client Secret: 21Vbk2sgr2qj99Lyqud0GOgiLBar0Z4X
```

**Пояснение:** Это секретный ключ, который Nextcloud будет использовать для подтверждения своей подлинности при обмене с Keycloak. Храните его в безопасности!

### 1.4. Настройка ролей и прав администратора

#### 1.4.1. Создание клиентской роли `admin`

**Навигация:**
- **Clients** → выберите **nextcloud** → вкладка **Roles**
- Нажмите **Create role**
- **Role name:** `admin`
- Нажмите **Save**

#### 1.4.2. Назначение роли пользователю

**Навигация:**
- **Users** → выберите нужного пользователя (например, `k@runtel.ru`)
- Откройте вкладку **Role mapping**
- Нажмите **Assign role** → выберите **Filter by clients** → укажите **nextcloud**
- Выберите роль **`admin`** и нажмите **Assign**

#### 1.4.3. Добавление маппера для передачи роли в токен

**Навигация:**
- **Clients** → выберите **nextcloud**
- Перейдите на вкладку **Client scopes**
- Нажмите на **`nextcloud-dedicated`**
- Нажмите **Add mapper** → **By configuration**
- Выберите тип **`User Client Role`**

**Заполните поля маппера:**

| Поле | Значение | Пояснение |
|------|----------|-----------|
| Name | `Client Roles for Nextcloud` | Имя маппера |
| Client ID | `nextcloud` | Выберите из списка |
| Token Claim Name | `nextcloud-roles` | Имя поля, по которому Nextcloud будет искать группы |
| Claim JSON Type | `String` | Тип данных |
| Add to ID token | `ON` | Добавлять в ID токен |
| Add to access token | `ON` | Добавлять в access токен |
| Add to userinfo | `ON` | Добавлять в userinfo |
| Add to token response | `OFF` | Не требуется |

Нажмите **Save**.

**Результат:** В токене появится поле:
```json
"nextcloud-roles": ["admin"]
```

---

## Часть 2. Настройка Nextcloud (Сторона Service Provider)

### 2.1. Установка приложения OpenID Connect

**Команда:**
```bash
sudo -u www-data php /var/www/html/nextcloud/occ app:install user_oidc
```

**Результат:**
```
user_oidc 8.10.1 installed
user_oidc enabled
```

### 2.2. Создание провайдера аутентификации

**Команда (правильный синтаксис для v33.0.2):**
```bash
sudo -u www-data php /var/www/html/nextcloud/occ user_oidc:provider \
  --clientid="nextcloud" \
  --clientsecret="21Vbk2sgr2qj99Lyqud0GOgiLBar0Z4X" \
  --discoveryuri="https://sso.runtel.ru:8443/realms/runtel/.well-known/openid-configuration" \
  --scope="openid profile email" \
  "Keycloak"
```

### 2.3. Включение групповой синхронизации

**Команда:**
```bash
sudo -u www-data php /var/www/html/nextcloud/occ user_oidc:provider \
  --group-provisioning="1" \
  --mapping-groups="nextcloud-roles" \
  "Keycloak"
```

### 2.4. Проверка создания провайдера

**Команда:**
```bash
sudo -u www-data php /var/www/html/nextcloud/occ user_oidc:provider
```

**Результат:**
```
+----+------------+---------------------------------------------------------------------------+----------------------+-----------+
| ID | Identifier | Discovery endpoint                                                        | End session endpoint | Client ID |
+----+------------+---------------------------------------------------------------------------+----------------------+-----------+
| 1  | Keycloak   | https://sso.runtel.ru:8443/realms/runtel/.well-known/openid-configuration |                      | nextcloud |
+----+------------+---------------------------------------------------------------------------+----------------------+-----------+
```

### 2.5. Детальный просмотр конфигурации (проверка маппинга)

**Команда:**
```bash
sudo -u www-data php /var/www/html/nextcloud/occ user_oidc:provider "Keycloak" | grep -E "mappingGroups|groupProvisioning|uniqueUid"
```

**Ожидаемый результат:**
```json
"mappingGroups":"nextcloud-roles",
"groupProvisioning":true,
"uniqueUid":true
```

### 2.6. Проверка установленных приложений

**Команда:**
```bash
sudo -u www-data php /var/www/html/nextcloud/occ app:list | grep oidc
```

**Результат:**
```
  - oidc: 1.16.6
  - user_oidc: 8.10.1
```

### 2.7. Очистка кеша и перезапуск сервисов

```bash
sudo -u www-data php /var/www/html/nextcloud/occ maintenance:repair
sudo systemctl restart php8.3-fpm nginx
```

---

## Часть 3. Проверка работы SSO

### 3.1. Тестирование входа через SSO

1. Выйдите из Nextcloud
2. Откройте страницу входа: `https://nextcloud.runtel.ru/login`
3. Нажмите кнопку **"Log in with Keycloak"**
4. Введите учётные данные пользователя Keycloak
5. После успешного входа вы вернётесь в Nextcloud

### 3.2. Проверка пользователей в системе

**Команда:**
```bash
sudo -u www-data php /var/www/html/nextcloud/occ user:list
```

### 3.3. Добавление существующих пользователей в группу admin

**Для уже существующих пользователей (созданных до включения групповой синхронизации):**

```bash
# Найти uid пользователя
sudo -u www-data php /var/www/html/nextcloud/occ user:list

# Добавить в группу admin (пример для Кирилла)
sudo -u www-data php /var/www/html/nextcloud/occ group:adduser admin "9e7973820ad23343aa1b6b682a50e8472235c87c4249c1e21373b715acd79c82"
```

### 3.4. Проверка через веб-интерфейс

**Ссылки для проверки:**
- Настройки OIDC: `https://nextcloud.runtel.ru/settings/admin/user_oidc`
- Группа admin: `https://nextcloud.runtel.ru/apps/contacts/user_group/admin`
- Управление пользователями: `https://nextcloud.runtel.ru/settings/admin/users`

---

## Итог

**Что мы настроили в Keycloak:**
1. Клиент `nextcloud` с правильными URL перенаправления
2. Client Secret для безопасного обмена
3. Роль `admin` для клиента `nextcloud`
4. Маппер для передачи роли в поле `nextcloud-roles`
5. Назначение роли `admin` пользователям

**Что мы настроили в Nextcloud:**
1. Приложение `user_oidc`
2. Провайдер `Keycloak` с Discovery URI
3. Групповую синхронизацию через поле `nextcloud-roles`
4. Добавили существующих пользователей в группу `admin`

**Результат:** Пользователи успешно входят через Keycloak и получают права администратора в Nextcloud.
