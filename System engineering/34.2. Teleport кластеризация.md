# Полное руководство по развертыванию HA-кластера Teleport Enterprise v16

В данном руководстве описан процесс настройки отказоустойчивого кластера Teleport с использованием внешней БД PostgreSQL, балансировщика HAProxy и интеграции с Keycloak (OIDC).

---

## 1. Подготовка инфраструктуры

| Узел | IP-адрес | Роль |
|-----------|----------|------|
| **teleport1** | 192.168.87.238 | Auth + Proxy + App |
| **teleport2** | 192.168.87.239 | Auth + Proxy + App |
| **pg1 (Leader)** | 192.168.45.201 | PostgreSQL (порт 5000 через HAProxy) |
| **pg2 (Replica)** | 192.168.45.202 | PostgreSQL (реплика) |
| **pg3 (Replica)** | 192.168.45.204 | PostgreSQL (реплика) |
| **HAProxy** | 192.168.45.201 | Балансировщик (порт 5000 для БД, порты 443/3023/3080 для Teleport) |

---

## 2. Настройка базы данных PostgreSQL

Teleport v16 требует логической репликации для синхронизации кэша между нодами.

### 2.1. Установка wal2json (обязательно!)

```bash
# На всех PostgreSQL серверах
apt install postgresql-15-wal2json
systemctl restart postgresql
```

### 2.2. Создание БД и пользователя

Зайдите на мастер-ноду PostgreSQL и выполните:

```bash
sudo -u postgres psql
```

```sql
-- Создаем пользователя
CREATE USER teleport_user WITH PASSWORD 'VeryStrong77!';

-- Создаем базу данных
CREATE DATABASE teleport OWNER teleport_user;

-- Даем права
GRANT ALL PRIVILEGES ON DATABASE teleport TO teleport_user;

-- ВАЖНО: Для работы Replication Slots (Change Feed) в v16 Enterprise
-- пользователю требуются права SUPERUSER или REPLICATION
ALTER USER teleport_user WITH REPLICATION;

-- Проверяем созданных пользователей
\du
```

**Вывод команды `\du`:**

```
                                             Список ролей
   Имя роли    |                                Атрибуты                                 | Член ролей 
---------------+-------------------------------------------------------------------------+------------
 jira          |                                                                         | {}
 keycloak      |                                                                         | {}
 postgres      | Суперпользователь, Создаёт роли, Создаёт БД, Репликация, Пропускать RLS | {}
 replicator    | Репликация                                                              | {}
 rt_pbx        |                                                                         | {}
 teleport_user | Репликация                                                              | {}
```

### 2.3. Настройка .pgpass для автоматизации

Чтобы утилиты могли подключаться к БД без ввода пароля:

```bash
# На всех серверах (teleport1, teleport2, pg1)
cat >> ~/.pgpass <<EOF
192.168.45.201:5000:teleport:teleport_user:VeryStrong77!
EOF
chmod 600 ~/.pgpass
```

### 2.4. Установка прав на файл .pgpass

Права должны быть строго `600`, иначе PostgreSQL игнорирует файл:

```bash
chmod 600 ~/.pgpass
```

### 2.5. Проверка подключения к PostgreSQL через HAProxy

```bash
psql -h 192.168.45.201 -p 5000 -U teleport_user -d teleport -c "SELECT 1"
```

**Ожидаемый вывод:**

```
 ?column?
----------
        1
(1 строка)
```

### 2.6. Просмотр таблиц в базе данных Teleport

```bash
psql -h 192.168.45.201 -p 5000 -U teleport_user -d teleport -c "\dt"
```

**Ожидаемый вывод:**

```
                  Список отношений
 Схема  |       Имя       |   Тип   |   Владелец    
--------+-----------------+---------+---------------
 public | backend_version | таблица | teleport_user
 public | kv              | таблица | teleport_user
(2 строки)
```

**Пояснение:** Teleport хранит все данные (пользователей, роли, сертификаты) в JSONB формате внутри таблицы `kv`. Таблица `backend_version` хранит версию схемы данных.

### 2.7. Просмотр данных о пользователях в БД

```bash
psql -h 192.168.45.201 -p 5000 -U teleport_user -d teleport -c "SELECT key FROM kv WHERE key LIKE '%user%' LIMIT 5;"
```

**Ожидаемый вывод (пример):**

```
                                                  key
--------------------------------------------------------------------------------------------------------
 \x2f7765622f75736572732f74657374757365722f706172616d73
 \x2f75736572746f6b656e2f37613764353464366662626562326539366162633838363466363538326164622f706172616d73
 \x2f617574686f7269746965732f757365722f74656c65706f72742d72756e74656c
 \x2f757365725f6c6f67696e5f73746174652f7465737475736572
 \x2f757365725f6c6f67696e5f73746174652f4074656c65706f72742d6163636573732d617070726f76616c2d626f74
(5 строк)
```

### 2.8. Просмотр всех баз данных в кластере

```bash
psql -h 192.168.45.201 -p 5000 -U postgres -c "\l"
```

### 2.9. Просмотр информации о подключениях

```bash
psql -h 192.168.45.201 -p 5000 -U postgres -c "SELECT pid, usename, application_name, client_addr, state FROM pg_stat_activity WHERE datname = 'teleport';"
```

---

## 3. Настройка первого узла (teleport1)

### 3.1. Редактирование /etc/teleport.yaml

```bash
mcedit /etc/teleport.yaml
```

**Содержимое файла:**

```yaml
version: v3
teleport:
  auth_token: /var/lib/teleport/token
  ca_pin: ######
  nodename: teleport.runtel.org
  advertise_ip: 192.168.87.238
  cache:
    type: in-memory
  log:
    output: stderr
    severity: DEBUG
  data_dir: /var/lib/teleport
  storage:
    type: postgres
    conn_string: "postgres://teleport_user:VeryStrong77!@192.168.45.201:5000/teleport?sslmode=disable"
  auth_server: 192.168.87.238:3025

auth_service:
  enabled: yes
  license_file: /var/lib/teleport/license.pem
  keep_alive_interval: 1m
  keep_alive_count_max: 3
  listen_addr: 0.0.0.0:3025
  public_addr: 192.168.87.238:3025
  proxy_listener_mode: multiplex
  authentication:
    type: oidc
    second_factor: on
    webauthn:
      rp_id: teleport.runtel.org
  cluster_name: teleport-runtel

windows_desktop_service:
  enabled: yes
  listen_addr: 192.168.87.238:3389
  static_hosts:
    - name: win10
      ad: false
      addr: 192.168.87.114
      labels:
        datacenter: teleport-runtel

ssh_service:
  enabled: no

proxy_service:
  enabled: yes
  listen_addr: 0.0.0.0:3023
  tunnel_listen_addr: 0.0.0.0:3080
  web_listen_addr: 0.0.0.0:443
  public_addr: teleport.runtel.org:443
  ssh_public_addr: teleport.runtel.org:3023
  tunnel_public_addr: teleport.runtel.org:443
  https_keypairs:
    - cert_file: /etc/letsencrypt/live/teleport.runtel.org/fullchain.pem
      key_file: /etc/letsencrypt/live/teleport.runtel.org/privkey.pem

app_service:
  enabled: yes
  apps:
    - name: jenkins
      uri: http://192.168.87.11:8080
      public_addr: jenkins.runtel.ru
    - name: jira
      uri: http://192.168.46.4:8080
      public_addr: jira.runtel.ru
    - name: gitlab
      uri: https://gitlab.runtel.org
      public_addr: gitlab.runtel.org
    - name: passbolt
      uri: https://passwd.runtel.ru
      public_addr: passwd.runtel.ru
    - name: grafana
      uri: http://192.168.87.209:3000
      public_addr: grafana.teleport.runtel.org
```

### 3.2. Запуск и проверка

```bash
systemctl restart teleport
ss -tlnp | grep 3025   # Должен быть LISTEN
ss -tlnp | grep -E "443|3023|3080"   # Прокси порты
```

---

## 4. Настройка OIDC (Keycloak)

В v16 коннекторы нельзя описывать в `teleport.yaml`. Их нужно создавать как динамические ресурсы.

### 4.1. Создание манифеста коннектора

```bash
mkdir -p /etc/teleport.d/
mcedit /etc/teleport.d/keycloak-connector.yaml
```

**Содержимое `keycloak-connector.yaml`:**

```yaml
kind: oidc
version: v3
metadata:
  name: keycloak
spec:
  client_id: teleport
  client_secret: "HgO2LUuSb7zKvtxgL2sG5aEErjp1ddop"
  issuer_url: "https://sso.runtel.ru:8443/realms/runtel"
  redirect_url: "https://teleport.runtel.org/v1/webapi/oidc/callback"
  scope: ["openid", "profile", "email", "groups"]
  claims_to_roles:
    - claim: groups
      value: jira-administrators
      roles: ["access", "editor"]
    - claim: groups
      value: confluence-administrators
      roles: ["access", "editor"]
    - claim: groups
      value: jenkins_admin
      roles: ["access", "editor"]
    - claim: email
      value: "k@runtel.ru"
      roles: ["access", "editor"]
    - claim: groups
      value: engineers
      roles: ["access"]

    # Инженеры и пользователи (только доступ)
    - claim: groups
      value: engineers
      roles: ["access"]
    - claim: groups
      value: support
      roles: ["access"]
    - claim: groups
      value: jira-software-users
      roles: ["access"]
    - claim: groups
      value: confluence-users
      roles: ["access"]
    - claim: groups
      value: jira-servicedesk-users
      roles: ["access"]
    - claim: groups
      value: default-roles-runtel
      roles: ["access"]
```

### 4.2. Применение коннектора

```bash
tctl create -f /etc/teleport.d/keycloak-connector.yaml
```

### 4.3. Проверка создания пользователя после входа

```bash
tctl get users
```

**Ожидаемый вывод (появление пользователя из Keycloak):**

```yaml
kind: user
metadata:
  name: k@runtel.ru
spec:
  created_by:
    connector:
      id: keycloak
      type: oidc
  roles:
  - access
  - editor
```

---

## 5. Настройка второго узла (teleport2)

Второй узел должен быть клоном первого, но с уникальным именем и IP.

### 5.1. Копирование контейнера (на PVE хосте)

```bash
pct stop <ID_teleport1>
pct clone <ID_teleport1> <новый_ID> --hostname teleport2 --full
pct start <ID_teleport1> <новый_ID>
```

### 5.2. Настройка IP и hostname на teleport2

```bash
# На teleport2
mcedit /etc/teleport.yaml
```

```yaml
teleport:
  nodename: teleport2.runtel.org
  advertise_ip: 192.168.87.239
  storage:
    type: postgres
    conn_string: "postgres://teleport_user:VeryStrong77!@192.168.45.201:5000/teleport?sslmode=disable"

auth_service:
  public_addr: 192.168.87.239:3025
  authentication:
    type: oidc
    second_factor: on
    webauthn:
      rp_id: teleport.runtel.org

windows_desktop_service:
  listen_addr: 192.168.87.239:3389
```

### 5.3. Очистка локальных данных и запуск

```bash
systemctl stop teleport
rm -rf /var/lib/teleport/backend/*
rm -rf /var/lib/teleport/proc/*
systemctl start teleport
```

---

## 6. Балансировка через HAProxy

### 6.1. Конфигурация HAProxy для PostgreSQL и Teleport

```bash
mcedit /etc/haproxy/haproxy.cfg
```

```haproxy
global
    log /dev/log local0
    log /dev/log local1 notice
    chroot /var/lib/haproxy
    stats socket /run/haproxy/admin.sock mode 660 level admin
    stats timeout 30s
    user haproxy
    group haproxy
    daemon

defaults
    log global
    mode tcp
    option tcplog
    option dontlognull
    timeout connect 5000
    timeout client 0
    timeout server 0

listen stats
    bind *:7000
    mode http
    stats enable
    stats uri /
    stats refresh 10s
    stats admin if TRUE

backend postgres_master
    mode tcp
    balance roundrobin
    option httpchk GET /master
    http-check expect status 200
    default-server inter 3s fall 3 rise 2 on-marked-down shutdown-sessions
    server pg1 192.168.45.201:5432 check port 8008 inter 5s rise 2 fall 3
    server pg2 192.168.45.202:5432 check port 8008 inter 5s rise 2 fall 3
    server pg3 192.168.45.204:5432 check port 8008 inter 5s rise 2 fall 3

backend postgres_replica
    mode tcp
    balance roundrobin
    option httpchk GET /replica
    http-check expect status 200
    default-server inter 3s fall 3 rise 2 on-marked-down shutdown-sessions
    server pg1 192.168.45.201:5432 check port 8008 inter 5s rise 2 fall 3
    server pg2 192.168.45.202:5432 check port 8008 inter 5s rise 2 fall 3
    server pg3 192.168.45.204:5432 check port 8008 inter 5s rise 2 fall 3

frontend postgres_frontend
    bind *:5000
    mode tcp
    default_backend postgres_master

frontend postgres_readonly
    bind *:5001
    mode tcp
    default_backend postgres_replica


## TELEPORT
# from /etc/teleport.yaml

frontend teleport-web
    bind *:443
    mode tcp
    default_backend teleport-proxy-web

frontend teleport-ssh
    bind *:3023
    mode tcp
    default_backend teleport-proxy-ssh

frontend teleport-tunnel
    bind *:3080
    mode tcp
    default_backend teleport-proxy-tunnel

frontend teleport-auth
    bind *:3025
    mode tcp
    default_backend teleport-auth-backend

backend teleport-proxy-web
    mode tcp
    balance source
    option tcp-check
    server teleport1 192.168.87.238:443 check
    server teleport2 192.168.87.239:443 check

backend teleport-proxy-ssh
    mode tcp
    balance source
    option tcp-check
    server teleport1 192.168.87.238:3023 check
    server teleport2 192.168.87.239:3023 check

backend teleport-proxy-tunnel
    mode tcp
    balance source
    option tcp-check
    server teleport1 192.168.87.238:3080 check
    server teleport2 192.168.87.239:3080 check

backend teleport-auth-backend
    mode tcp
    balance source
    server teleport1 192.168.87.238:3025 check
    server teleport2 192.168.87.239:3025 check

```

### 6.2. Запуск HAProxy

```bash
haproxy -c -f /etc/haproxy/haproxy.cfg
systemctl restart haproxy
```
Статистика по адресу http://192.168.45.201:7000/

---

## 7. Настройка DNS / etc/hosts

### 7.1. На серверах Teleport

```bash
cat >> /etc/hosts <<EOF
192.168.87.238 teleport.runtel.org
192.168.87.238 teleport1
192.168.87.239 teleport2
192.168.45.201 pg1
192.168.45.202 pg2
192.168.45.204 pg3
EOF
```

### 7.2. На рабочей машине (для теста)

```bash
cat >> /etc/hosts <<EOF
192.168.45.201 teleport.runtel.org
192.168.87.238 teleport1
192.168.87.239 teleport2
EOF
```

**Важно:** `teleport.runtel.org` должен указывать на **HAProxy** (`192.168.45.201`), а не напрямую на teleport1.

---

## 8. Диагностика и полезные команды

### 8.1. Проверка статуса кластера

```bash
tctl status
tctl get nodes
tctl get users
```

### 8.2. Проверка логов Teleport

```bash
journalctl -u teleport -f
journalctl -u teleport -n 100 --no-pager | grep -iE "error|oidc|postgres"
```

### 8.3. Проверка соединения с PostgreSQL

```bash
psql -h 192.168.45.201 -p 5000 -U teleport_user -d teleport -c "SELECT 1"
psql -h 192.168.45.201 -p 5000 -U teleport_user -d teleport -c "\dt"
```

### 8.4. Проверка таблиц в БД

```bash
psql -h 192.168.45.201 -p 5000 -U teleport_user -d teleport -c "\dt"
```

Должны быть таблицы:
- `backend_version` — версия схемы
- `kv` — key-value хранилище данных Teleport

### 8.5. Просмотр пользователей в БД

```bash
psql -h 192.168.45.201 -p 5000 -U teleport_user -d teleport -c "SELECT key FROM kv WHERE key LIKE '%user%' LIMIT 10;"
```

### 8.6. Проверка синхронизации пользователей

```bash
# На teleport1
tctl users add testuser --roles=editor

# На teleport2
tctl users ls | grep testuser   # Должен появиться
```

### 8.7. Проверка логов OIDC при логине

```bash
journalctl -u teleport -f | grep -iE "oidc|claims|groups"
```

Ищите секцию `OIDC ID Token claims` — там будет список групп, которые прислал Keycloak.

### 8.8. Проверка портов

```bash
ss -tlnp | grep -E "443|3023|3080|3025|5000|7000"
```

### 8.9. Проверка HAProxy статистики

Откройте в браузере:
```
http://192.168.45.201:7000
```

### 8.10. Просмотр всех ролей в PostgreSQL

```bash
psql -h 192.168.45.201 -p 5000 -U postgres -c "\du"
```

### 8.11. Просмотр активных подключений к БД

```bash
psql -h 192.168.45.201 -p 5000 -U postgres -c "SELECT pid, usename, application_name, client_addr, state FROM pg_stat_activity;"
```

---

## 9. Типичные проблемы и решения

| Проблема | Решение |
|----------|---------|
| `field oidc not found in type config.Auth` | В v16 OIDC коннектор создаётся через отдельный файл, не в `teleport.yaml` |
| `must be superuser or replication role` | Выдайте пользователю права `ALTER USER teleport_user WITH REPLICATION;` |
| `could not access file "wal2json"` | Установите пакет `postgresql-15-wal2json` |
| `second_factor cannot be off` | Установите `second_factor: on` и настройте `webauthn` |
| `listen tcp 192.168.87.238:3025: bind: cannot assign requested address` | Исправьте `listen_addr` и `public_addr` на IP текущего сервера |
| `redirect_uri mismatch` | Добавьте в Keycloak точный URL: `https://teleport.runtel.org/v1/webapi/oidc/callback` |
| `tctl must be used on an Auth Service host` | Убедитесь, что Teleport запущен и порт 3025 слушается |
| `Пароль запрашивается, хотя есть .pgpass` | Проверьте права: `chmod 600 ~/.pgpass`. Убедитесь, что порт в файле совпадает с подключаемым |
| `.pgpass` игнорируется при подключении к HAProxy | Добавьте строку с портом `5000`, а не `5432` |

---

## 10. Итоговая архитектура

```
Пользователь
    │
    ▼
HAProxy (192.168.45.201)
    │
    ├── :443  → Teleport Web UI
    ├── :3023 → Teleport SSH
    ├── :3080 → Teleport Tunnel
    ├── :5000 → PostgreSQL Cluster (Patroni)
    └── :7000 → HAProxy Stats
    │
    ├── teleport1 (192.168.87.238) ←───┐
    │   ├── Auth Service (3025)       │
    │   ├── Proxy Service (443/3023/3080) │
    │   └── App Service               │
    │                                 │
    └── teleport2 (192.168.87.239)    │
        ├── Auth Service (3025)       │
        ├── Proxy Service             │
        └── App Service               │
                                      │
        ┌─────────────────────────────┘
        │
        ▼
PostgreSQL Cluster (Patroni)
    ├── pg1 (192.168.45.201) - Leader
    ├── pg2 (192.168.45.202) - Replica
    └── pg3 (192.168.45.204) - Replica
```

---

## 11. Работа с PostgreSQL через psql (справочник)

### Подключение к БД

```bash
# Подключение к HAProxy (балансировщик)
psql -h 192.168.45.201 -p 5000 -U teleport_user -d teleport

# Подключение напрямую к мастеру
psql -h 192.168.45.201 -p 5432 -U teleport_user -d teleport

# Подключение от суперпользователя
psql -h 192.168.45.201 -p 5000 -U postgres
```

### Основные команды psql

| Команда | Описание |
|---------|----------|
| `\l` | Список всех баз данных |
| `\dt` | Список таблиц в текущей БД |
| `\du` | Список пользователей/ролей |
| `\c teleport` | Переключиться на БД teleport |
| `\d kv` | Описание структуры таблицы kv |
| `\x` | Включить расширенный вывод (построчный) |
| `\q` | Выйти из psql |
| `\?` | Справка по командам |
| `\h` | Справка по SQL командам |

### Просмотр данных Teleport

```bash
# Просмотр ключей пользователей
SELECT key FROM kv WHERE key LIKE '%user%' LIMIT 10;

# Просмотр всех ключей
SELECT key FROM kv LIMIT 20;

# Подсчет количества записей
SELECT COUNT(*) FROM kv;

# Просмотр значения по ключу (в сыром виде)
SELECT value FROM kv WHERE key = '/web/users/testuser/params';
```

---

## 12. Заключение

Получен полностью отказоустойчивый кластер Teleport v16 Enterprise с:

- ✅ Двумя Auth/Proxy серверами
- ✅ Общей PostgreSQL БД (Patroni + HAProxy)
- ✅ Балансировкой HAProxy
- ✅ OIDC аутентификацией через Keycloak
- ✅ Доступом к приложениям (Jenkins, Jira, GitLab, Grafana, Passbolt) через Teleport

**Кластер готов к эксплуатации!** 🚀
