# OpenSIPS: Компиляция и установка на Debian12.

Прежде всего смотрим документацию вендора:
- [OpenSIPS/opensips/blob/master/INSTALL](https://github.com/OpenSIPS/opensips/blob/master/INSTALL)
- [Documentation/Install-CompileAndInstall-3-6](https://www.opensips.org/Documentation/Install-CompileAndInstall-3-6)


### 🔍 Ключевые моменты из файла INSTALL

1.  **Ошибка с опцией `-l` в 4.0.0**:
    Опция `-l` (список модулей), с которой у вас возникли проблемы, **возможно, устарела или изменилась в новой ветке разработки (master/4.0)**. Документ ссылается на веб-сайт как на основной источник актуальной документации.

2.  **Критически важное правило для сборки**:
    В разделе **2. Howto Build** есть важнейшее замечание (Note), которое объясняет вашу главную проблему:
    > **Если вы используете параметр `prefix` в команде `make install`, то вам также необходимо использовать этот параметр во всех предыдущих командах `make`, таких как `make`, `make modules` или `make all`.**

    Это объясняет, почему у вас получилась версия 4.0.0-dev: вы собрали (`make all`) с дефолтным префиксом (`/usr/local`), но возможно, ранее устанавливали (или в системе остались файлы от) другой версии, собранной с другими параметрами. Система могла найти старые объектные файлы или библиотеки.

3.  **Полная очистка (`make proper`)**:
    В разделе **Make targets** указано, что `make proper` очищает также зависимости. Это **обязательный шаг** перед переключением между версиями (например, с master на тег 3.6.3).

### 🛠️ Четкий план действий для сборки OpenSIPS 3.6.3

Исходя из инструкций вендора, вот что нужно сделать:

1.  **Вернитесь к исходникам и убедитесь, что на правильном теге**:
    ```bash
    cd /usr/src/opensips
    git checkout 3.6.3
    ```

2.  **Выполните ПОЛНУЮ очистку, как указано в документе**:
    ```bash
    make proper
    ```
    Это удалит все, что осталось от предыдущей сборки (включая зависимости). Без этого шага сборка может вести себя непредсказуемо.

3.  **Соберите и установите, явно указав префикс во ВСЕХ командах**:
    Следуя правилу из инструкции, выполните:
    ```bash
    make prefix=/usr/local all -j$(nproc)
    make prefix=/usr/local install
    ```
    Это гарантирует, что пути будут согласованы на всех этапах.

4.  **Проверьте результат**:
    ```bash
    /usr/local/sbin/opensips -V
    ```
    Теперь должно отобразиться `version: opensips 3.6.3`.

### 📝 Вывод и рекомендации

*   **Основная причина**: Вероятно, в вашей системе произошло смешивание файлов от разных сборок из-за того, что не соблюдалось правило единого префикса и не выполнялась полная очистка (`make proper`) при переключении версий.
*   **Решение**: Четко следуйте цепочке `git checkout` -> `make proper` -> сборка и установка с одинаковыми параметрами.
*   **Дальнейшие шаги**: После успешной установки 3.6.3 для управления сервером вам понадобится утилита `opensips-cli` (ссылка на её репозиторий есть в файле INSTALL).

Если после выполнения этих шагов проблема с версией или опцией `-l` сохранится, дайте знать — будем искать причину глубже.
